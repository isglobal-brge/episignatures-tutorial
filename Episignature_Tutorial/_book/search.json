[{"path":"index.html","id":"preface","chapter":"Preface","heading":"Preface","text":"book practical tutorial discovering testing DNA methylation episignatures rare disease. written readers different levels computational experience: bioinformaticians can follow analysis structure, geneticists non-specialist readers can use text understand step matters.developed Bioinformatics Research Group Genetic Epidemiology, ISGlobal practical companion manuscript chapter episignatures machine learning rare diseases.tutorial follows two complementary worked examples:Williams syndrome — de novo discovery: scripts/Williams_analysis.R uses GSE119778 identify DMPs, select candidate episignature CpG panel, train classifiers, evaluate samples kept discovery. independent GSE66552 cohort used external testing (Kimura et al. 2020; Strong et al. 2015).Kabuki syndrome — existing signature validation: scripts/Kabuki/ starts established Kabuki CpG panel, trains tutorial SVM using confirmed samples GSE97362, evaluates independent array cohorts (GSE116300, GSE218186), tests whether disease-associated signal remains detectable ONT PacBio methylation profiles long-read study (Butcher et al. 2017; Sobreira et al. 2017; Hildonen et al. 2026).code chunks disabled default book can rendered without downloading IDAT files loading large .rds objects. chapters intended read order relevant code run interactively companion scripts. scripts originated exploratory analyses, tutorial emphasizes dependencies, input checks, order discovery validation steps occur.research teaching workflow, clinical diagnostic report. Episignature interpretation requires appropriate reference cohorts, technical QC, independent validation, cross-disease specificity assessment possible, careful consideration platform biological limitations (Aref-Eshghi et al. 2020; Kerkhof et al. 2024).","code":""},{"path":"index.html","id":"how-to-render","chapter":"Preface","heading":"0.1 How to render","text":"R:run small wrapper:","code":"\nsetwd(\"scripts/bookdown_episignatures_tutorial\")\nbookdown::render_book(\n  \"index.Rmd\",\n  output_format = \"bookdown::bs4_book\",\n  params = list(run_code = FALSE)\n)cd scripts/bookdown_episignatures_tutorial\nRscript render_book.R"},{"path":"index.html","id":"learning-goals","chapter":"Preface","heading":"0.2 Learning goals","text":"end tutorial, readers able :explain difference localized methylation abnormality multivariate disease-associated episignature;recognize main objects produced Illumina methylation-array preprocessing;follow sequence sample/probe QC DMP discovery, CpG selection, model training, independent testing;understand feature selection must kept separate validation data;distinguish de novo episignature discovery testing established CpG panel;interpret classifier results together PCA, disease comparators, platform effects.","code":""},{"path":"index.html","id":"repository-paths-used-in-the-examples","chapter":"Preface","heading":"0.3 Repository paths used in the examples","text":"book assumes rendered scripts/bookdown_episignatures_tutorial/.Outside original server environment, set params$project_root folder contains Williams/ Kabuki/ analysis directories. tutorial constructs dataset-specific paths root rather relying original absolute HPC paths.","code":"\nproject_root\npath_williams\npath_kabuki\npath_discovery\npath_external_williams\npath_kabuki_training\npath_kabuki_long_read"},{"path":"primer.html","id":"primer","chapter":"1 Primer for New Readers","heading":"1 Primer for New Readers","text":"chapter gives minimum background needed follow workflow. written readers may new DNA methylation analysis, emphasis step , needed, connects episignature discovery testing.","code":""},{"path":"primer.html","id":"clinical-epigenomics-in-rare-disease","chapter":"1 Primer for New Readers","heading":"1.1 Clinical epigenomics in rare disease","text":"Rare diseases neurodevelopmental disorders can remain difficult diagnose even extensive genomic testing. patient may clearly causal variant, testing may identify variant uncertain significance (VUS) whose functional effect unclear. DNA methylation profiling provides complementary readout can capture downstream molecular consequences disease rather DNA sequence alone. growing number rare disorders, reproducible disease-associated methylation profiles can used episignatures support diagnosis variant interpretation (Aref-Eshghi et al. 2020; Kerkhof et al. 2024).tutorial prepared Bioinformatics Research Group Genetic Epidemiology, ISGlobal practical companion manuscript chapter episignatures rare disorders. uses public datasets demonstrate two complementary scenarios:Williams syndrome: deriving de novo episignature methylation-array data, selecting CpGs, training classifiers, testing external samples.Kabuki syndrome: starting established CpG panel evaluating whether array-trained models behave consistently across array long-read methylation data.","code":""},{"path":"primer.html","id":"glossary","chapter":"1 Primer for New Readers","heading":"1.2 Glossary","text":"Episignature: reproducible, disease-associated DNA methylation profile. practice, usually represented selected panel CpG sites whose combined pattern separates affected individuals appropriate reference samples. diagnostic information comes multivariate pattern, necessarily single CpG.Epivariant, epimutation, rare differentially methylated region: localized methylation abnormality affecting specific genomic region. different genome-wide episignature. Imprinting defects special case involve abnormal methylation parent--origin-specific regulatory regions may present broad episignature.Classifier score: numerical output machine-learning model. tutorial, main score model-derived probability assigned Case class. high low score can support interpretation, must considered together phenotype, genotype, sample quality, platform, reference data used train model.","code":""},{"path":"primer.html","id":"dna-methylation-in-one-page","chapter":"1 Primer for New Readers","heading":"1.3 DNA methylation in one page","text":"DNA methylation epigenetic modification methyl group added cytosine, commonly CpG sites. Methylation patterns can stably maintained cell division also changing development biological context. DNA methylation contributes gene regulation, cell identity, X-chromosome inactivation, genomic imprinting, repression repetitive elements (Jones 2012; Moore, Le, Fan 2013).beta-values used tutorial, methylation represented scale 0 1:0 means little methylation detected CpG.1 means methylation detected almost measured molecules CpG.Blood mixture cell types, cell type methylation profile. clinical episignature studies use peripheral blood accessible supported large reference datasets. negative blood-based result, however, prove disease-relevant methylation changes absent another tissue (Kerkhof et al. 2024).Illumina methylation arrays measure hundreds thousands predefined CpG sites across genome. raw scanner files IDATs, one red-channel file one green-channel file per sample. R package minfi can read files RGChannelSet, perform preprocessing normalization, map probes genome, produce GenomicRatioSet (GRset) containing methylation measurements (Aryee et al. 2014).Two transformations used throughout scripts:Beta-values: values 0 1 easy interpret relative methylation levels. tutorial used visualization classifier inputs.M-values: logit transformation beta-values. less intuitive plot, suitable statistical properties differential methylation modelling, particularly near beta-value extremes (Du et al. 2010).","code":""},{"path":"primer.html","id":"what-is-an-episignature","chapter":"1 Primer for New Readers","heading":"1.4 What is an episignature?","text":"episignature reproducible disease-associated DNA methylation pattern can function molecular biomarker. repository, term refers selected panel CpGs whose combined methylation profile can distinguish cases controls can used unsupervised visualization supervised classification (Aref-Eshghi et al. 2020).two related different tasks:Discovery asks: CpGs differ cases controls?Detection asks: given new sample, methylation profile look like case group?scripts move discovery detection:read normalize IDAT files;perform QC remove problematic samples/probes;estimate blood cell composition;use PCA inspect batch effects outliers;call differentially methylated positions limma (Ritchie et al. 2015);choose compact, non-redundant CpG panel;train classifier, usually SVM;test held-independent samples.","code":""},{"path":"primer.html","id":"cases-controls-and-why-matching-matters","chapter":"1 Primer for New Readers","heading":"1.5 Cases, controls, and why matching matters","text":"classifier learns differences present training data. cases controls also differ strongly age, sex, blood cell composition, chip, laboratory, array type, model may learn technical biological differences instead disease-associated signal. workflow therefore checks adjusts several potential confounders:sex prediction reported-sex checks;removal sex chromosome probes discovery necessary;estimated immune cell composition limma model;batch effect diagnosis using top variable CpGs, variable importance known confounders;held-validation samples external datasets.steps reduce risk, replace thoughtful cohort design. Feature selection model fitting must also performed using discovery/training samples ; using validation samples steps can produce overly optimistic performance estimates (Ambroise McLachlan 2002).","code":""},{"path":"primer.html","id":"why-blood-cell-composition-is-included","chapter":"1 Primer for New Readers","heading":"1.6 Why blood cell composition is included","text":"Blood mixture cell types, cell type characteristic methylation profile. cases controls different immune-cell proportions, differences can appear disease-associated methylation changes. Estimating cell composition including appropriate cell fractions covariates can reduce source confounding (Houseman et al. 2012; Salas et al. 2018).","code":""},{"path":"primer.html","id":"why-pca-appears-repeatedly","chapter":"1 Primer for New Readers","heading":"1.7 Why PCA appears repeatedly","text":"PCA used two distinct purposes:Technical QC: discovery, PCA run variable autosomal CpGs. goal see whether slide, array position, outlier samples dominate data.Episignature visualization: CpG selection, PCA rerun using episignature CpGs. goal see whether cases controls separate selected feature space.first PCA quality-control view: helps identify technical structure unusual samples need review. second PCA signature view: shows whether selected CpGs separate groups unsupervised projection. PCA descriptive prove diagnostic performance.","code":""},{"path":"primer.html","id":"what-an-svm-score-means","chapter":"1 Primer for New Readers","heading":"1.8 What an SVM score means","text":"caret SVM workflow used returns class probabilities (Kuhn 2008). main output column called Case ranges 0 1:scores near 0 control-like;scores near 1 case-like;intermediate scores require caution.threshold 0.5 convenient illustration, universal clinical cutoff. Thresholds score interpretation established independent validation data , possible, disease-control cohorts.","code":""},{"path":"primer.html","id":"discovery-panel-versus-validation-panel","chapter":"1 Primer for New Readers","heading":"1.9 Discovery panel versus validation panel","text":"Williams workflow discovers CpG panel GSE119778 tests held-samples independent GSE66552 cohort. Kabuki workflow different: starts established Kabuki CpG panel, stored scripts/Kabuki/PMID41225292/episignature_PMID41225292.xlsx, evaluates fixed signal across independent array long-read datasets (Aref-Eshghi et al. 2020; Hildonen et al. 2026). distinction central tutorial:Williams teaches discover validate new candidate signature.Kabuki teaches test existing signature across cohorts technologies.","code":""},{"path":"repo-map.html","id":"repo-map","chapter":"2 Repository Map","heading":"2 Repository Map","text":"chapter maps analysis scripts used throughout tutorial. intended simple reference: dataset, shows script prepares data, outputs created, outputs used later.","code":""},{"path":"repo-map.html","id":"top-level-helper","chapter":"2 Repository Map","heading":"2.1 Top-level helper","text":"scripts/functions_TutorialEpis.R contains shared functions:read_idat_minfi() reads IDATs, runs sample/probe detection P-value filters, predicts sex, normalizes, drops SNP cross-reactive probes, optionally estimates immune cell composition.detect_pca_outliers() identifies PC1/PC2 outliers Mahalanobis distance within Case_Cont groups.model wrappers svm_training_f(), elastic_net_training_f(), ranger_training_f(), knn_training_f(), others train binary classifiers caret.plotting helpers create SVM score plots, heatmaps, palettes.tutorial helper scripts/bookdown_episignatures_tutorial/R/functions_tutorial_epis.R follows general analysis logic makes paths explicit adds input checks useful public, step--step workflow. safeguards also address several portability first-run issues documented maintainer notes.","code":""},{"path":"repo-map.html","id":"williams-scripts","chapter":"2 Repository Map","heading":"2.2 Williams scripts","text":"","code":""},{"path":"repo-map.html","id":"scriptswilliams_analysis.r","chapter":"2 Repository Map","heading":"2.2.1 scripts/Williams_analysis.R","text":"main Williams episignature discovery workflow.sections :configure paths GSE119778;retrieve phenotype metadata GEO;read IDATs preprocess methylation arrays;estimate immune cell proportions;run PCA batch outlier detection;define discovery validation samples;run limma discovery samples;rank DMPs select episignature CpGs;train several classifiers beta-values M-values;score discovery, internal validation, external GSE66552 samples;export plots prediction tables.Important outputs include:Williams/GSE119778/GRset_GSE119778.rdsWilliams/GSE119778/Tables/DMPs_limma_GSE119778.xlsxWilliams/GSE119778/Tables/EpisignatureCpGs_GSE119778.xlsxWilliams/GSE119778/_classification_models_GSE119778.rdsWilliams/GSE119778/Tables/Classification_predictions_GSE119778.xlsx","code":""},{"path":"repo-map.html","id":"scriptswilliamsgse66552gse66552_analysis.r","chapter":"2 Repository Map","heading":"2.2.2 scripts/Williams/GSE66552/GSE66552_analysis.R","text":"script prepares independent Williams validation/comparator dataset GSE66552 (Strong et al. 2015). :parses GEO metadata typical controls, Williams syndrome, 7q11.23 duplication samples;preprocesses IDATs;runs PCA inspect technical diagnostic structure;subsets object Williams CpGs discovered GSE119778;saves GRset_epi_GSE66552.rds external scoring main Williams script.7q11.23 duplication samples comparators rather Williams cases useful checking whether learned methylation pattern specific deletion-associated Williams profile.","code":""},{"path":"repo-map.html","id":"kabuki-scripts","chapter":"2 Repository Map","heading":"2.3 Kabuki scripts","text":"","code":""},{"path":"repo-map.html","id":"scriptskabukigse97362kabuki_gse97362_analysis.r","chapter":"2 Repository Map","heading":"2.3.1 scripts/Kabuki/GSE97362/Kabuki_GSE97362_analysis.R","text":"script prepares array cohort used train Kabuki classifier. Unlike Williams workflow, discover new CpG panel. reads established Kabuki episignature panel stored :script:parses GEO metadata including sex, age, diagnosis, sample type;preprocesses IDATs read_idat_minfi();subsets data published Kabuki episignature CpGs;identifies confirmed discovery/training subset defined original Kabuki study;trains linear SVM fixed CpG panel;scores remaining samples exports prediction results.underlying Kabuki methylation cohort described Butcher et al. (Butcher et al. 2017).","code":"scripts/Kabuki/PMID41225292/episignature_PMID41225292.xlsx"},{"path":"repo-map.html","id":"scriptskabukigse116300kabuki_gse116300_analysis.r","chapter":"2 Repository Map","heading":"2.3.2 scripts/Kabuki/GSE116300/Kabuki_GSE116300_analysis.R","text":"prepares independent Kabuki cohort GSE116300, originally reported Sobreira et al. (Sobreira et al. 2017). script uses process_meth = \"quantile\" rather functional normalization compares IDAT-derived beta-values processed GEO values PCA. comparison helps reader see whether two preprocessing sources produce similar large-scale structure; make two preprocessing methods technically identical.","code":""},{"path":"repo-map.html","id":"scriptskabukigse218186kabuki_gse218186_analysis.r","chapter":"2 Repository Map","heading":"2.3.3 scripts/Kabuki/GSE218186/Kabuki_GSE218186_analysis.R","text":"prepares additional external Kabuki array dataset GSE218186. Reported sex available GEO metadata used script, predicted sex stored harmonized GRset_epis object. dataset used external testing rather defining Kabuki CpG panel.","code":""},{"path":"repo-map.html","id":"scriptskabukipmid41225292lr_analysis.r","chapter":"2 Repository Map","heading":"2.3.4 scripts/Kabuki/PMID41225292/LR_analysis.R","text":"script converts methylation matrix provided long-read Kabuki study GenomicRatioSet using makeGenomicRatioSetFromMatrix(). reads metadata rows Platform, Affected, Cohort, creates sample labels, saves GRset_epis_PMID41225292.rds (Hildonen et al. 2026).object bridge array-derived Kabuki episignature methylation estimates generated ONT PacBio, together matched array measurements included study.","code":""},{"path":"repo-map.html","id":"scriptskabukikabuki_analysis.r","chapter":"2 Repository Map","heading":"2.3.5 scripts/Kabuki/Kabuki_analysis.R","text":"combined Kabuki validation script. :loads SVM trained GSE97362;loads prepared GRset_epis_* objects;identifies CpGs shared across included datasets;checks CpGs required trained model available scoring;combines compatible objects GRset_common;labels training samples, external array samples, CHARGE/CHD7 comparators, ONT/PacBio samples;runs PCA Kabuki episignature CpGs;applies trained SVM;builds heatmap summary figures;compares long-read methylation values high-confidence array reference profiles.script therefore addresses two questions separately: whether Kabuki disease signal remains classifiable, whether numerical methylation values directly comparable across technologies.","code":""},{"path":"setup-data.html","id":"setup-data","chapter":"3 Setup and Data Organization","heading":"3 Setup and Data Organization","text":"repository uses simple organization: one folder per syndrome, one subfolder per dataset, predictable filenames phenotype tables, processed methylation objects, figures, exported results. Keeping structure consistent makes later code easier follow.","code":""},{"path":"setup-data.html","id":"recommended-folder-structure","chapter":"3 Setup and Data Organization","heading":"3.1 Recommended folder structure","text":"discovery dataset:external validation dataset:Kabuki follows pattern. long-read matched-array workbook Hildonen et al. study stored Kabuki/PMID41225292/ (Hildonen et al. 2026).","code":"Williams/GSE119778/\n  IDATs/\n  Tables/\n  Figures/\n  tmp_data/\n  pheno_GSE119778.csv\n  GRset_GSE119778.rdsWilliams/GSE66552/\n  IDATs/\n  Tables/\n  Figures/\n  pheno_GSE66552.csv\n  GRset_GSE66552.rds\n  GRset_epi_GSE66552.rds"},{"path":"setup-data.html","id":"install-packages","chapter":"3 Setup and Data Organization","heading":"3.2 Install packages","text":"workflow uses Bioconductor CRAN packages. Installation normally done analysis, every time book rendered.main package groups :Data access methylation preprocessing: GEOquery, minfi, FlowSorted.Blood.EPIC, maxprobes, Biobase, S4Vectors, SummarizedExperiment (Davis Meltzer 2007; Aryee et al. 2014).Differential methylation: limma (Ritchie et al. 2015).Data handling plotting: tidyverse plus plotting workbook packages listed installation chunk (Wickham et al. 2019).Machine learning: caret coordinates model training, packages e1071, glmnet, ranger, kknn, gbm, xgboost, nnet provide individual model backends (Kuhn 2008).Book rendering: bookdown (Xie 2016).complete installation list kept code readers can install dependencies without identify one one.analyses tutorial use set reusable wrapper functions methylation preprocessing, quality control, machine-learning model training, prediction, visualization. functions loaded R/functions_tutorial_epis.R. complete definitions additional explanations provided Chapter 7.","code":"\nif (!requireNamespace(\"BiocManager\", quietly = TRUE)) {\n  install.packages(\"BiocManager\")\n}\n\nBiocManager::install(c(\n  \"GEOquery\",\n  \"minfi\",\n  \"limma\",\n  \"FlowSorted.Blood.EPIC\",\n  \"ComplexHeatmap\",\n  \"S4Vectors\",\n  \"SummarizedExperiment\",\n  \"Biobase\",\n  \"maxprobes\"\n))\n\ninstall.packages(c(\n  \"tidyverse\",\n  \"caret\",\n  \"doParallel\",\n  \"e1071\",\n  \"glmnet\",\n  \"ranger\",\n  \"kknn\",\n  \"pls\",\n  \"klaR\",\n  \"gbm\",\n  \"xgboost\",\n  \"nnet\",\n  \"data.table\",\n  \"ggrepel\",\n  \"ggpubr\",\n  \"ggbeeswarm\",\n  \"pals\",\n  \"readxl\",\n  \"openxlsx\",\n  \"RColorBrewer\",\n  \"patchwork\",\n  \"bookdown\"\n))"},{"path":"setup-data.html","id":"load-packages-for-an-analysis-session","chapter":"3 Setup and Data Organization","heading":"3.3 Load packages for an analysis session","text":"","code":"\nlibrary(GEOquery)\nlibrary(minfi)\nlibrary(tidyverse)\nlibrary(limma)\nlibrary(caret)\nlibrary(doParallel)\nlibrary(ggrepel)\nlibrary(ggpubr)\nlibrary(readxl)\nlibrary(openxlsx)\n\nsource(file.path(\"R\", \"functions_tutorial_epis.R\"))\n\nif (file.exists(epiflare_utils)) {\n  source(epiflare_utils)\n}"},{"path":"setup-data.html","id":"define-paths","chapter":"3 Setup and Data Organization","heading":"3.4 Define paths","text":"examples build paths file.path() instead hard-coded server locations. makes clearer input output lives makes workflow easier move another computer.","code":"\ngeo_ID <- \"GSE119778\"\nNDD_syndrome <- \"Williams\"\n\npath_save <- file.path(project_root, NDD_syndrome, geo_ID)\npath_tmp <- file.path(path_save, \"tmp_data\")\npath_idat <- file.path(path_save, \"IDATs\")\npath_figures <- file.path(path_save, \"Figures\")\n\nensure_project_dirs(path_save)"},{"path":"setup-data.html","id":"download-geo-metadata","chapter":"3 Setup and Data Organization","heading":"3.5 Download GEO metadata","text":"GEO datasets use one universal set phenotype column names. safest approach inspect metadata first map dataset-specific fields labels used tutorial.","code":"\ngse <- GEOquery::getGEO(GEO = geo_ID, destdir = path_idat)[[1]]\ngeo_pheno_raw <- Biobase::pData(gse)\n\ncolnames(geo_pheno_raw)\ndplyr::glimpse(geo_pheno_raw)"},{"path":"setup-data.html","id":"harmonized-phenotype-schema","chapter":"3 Setup and Data Organization","heading":"3.6 Harmonized phenotype schema","text":"combined analyses, tutorial aims following common phenotype schema:every raw parser creates fields immediately. example, age unavailable Williams discovery cohort, dataset labels added preprocessing harmonization. important point columns required later analysis must exist analysis run.Case_Cont binary outcome used Williams limma model classifier training. Diagnosis Dataset retain detailed information interpretation plotting.","code":""},{"path":"setup-data.html","id":"williams-phenotype-parser","chapter":"3 Setup and Data Organization","heading":"3.7 Williams phenotype parser","text":"Williams discovery dataset GSE119778 comes Kimura et al. study (Kimura et al. 2020). script maps metadata tutorial labels follows:independent Williams validation/comparator dataset GSE66552, diagnosis field group:ch1, typical controls labelled TD control (Strong et al. 2015).","code":"\npheno_williams <- Biobase::pData(gse) %>%\n  dplyr::mutate(\n    Sex = dplyr::case_when(\n      `gender:ch1` == \"Male\" ~ \"Male\",\n      `gender:ch1` == \"Female\" ~ \"Female\",\n      TRUE ~ NA_character_\n    ),\n    Diagnosis = `diagnosis:ch1`,\n    Case_Cont = dplyr::case_when(\n      Diagnosis == \"Control\" ~ \"Control\",\n      TRUE ~ \"Case\"\n    )\n  ) %>%\n  dplyr::select(title, geo_accession, Sex, Case_Cont, Diagnosis)\n\nreadr::write_csv(\n  pheno_williams,\n  file.path(path_save, paste0(\"pheno_\", geo_ID, \".csv\"))\n)\ngeo_ID_external <- \"GSE66552\"\npath_save_external <- file.path(project_root, \"Williams\", geo_ID_external)\npath_idat_external <- file.path(path_save_external, \"IDATs\")\nensure_project_dirs(path_save_external)\n\ngse_external <- GEOquery::getGEO(GEO = geo_ID_external, destdir = path_idat_external)[[1]]\n\npheno_external <- Biobase::pData(gse_external) %>%\n  dplyr::mutate(\n    Sex = NA_character_,\n    Diagnosis = `group:ch1`,\n    Case_Cont = dplyr::case_when(\n      Diagnosis == \"TD control\" ~ \"Control\",\n      TRUE ~ \"Case\"\n    )\n  ) %>%\n  dplyr::select(title, geo_accession, Sex, Case_Cont, Diagnosis)"},{"path":"setup-data.html","id":"kabuki-phenotype-parsers","chapter":"3 Setup and Data Organization","heading":"3.8 Kabuki phenotype parsers","text":"Kabuki workflow combines several datasets, one needs metadata parser.GSE97362, described Butcher et al. (Butcher et al. 2017):GSE116300, described Sobreira et al. (Sobreira et al. 2017), script treats unrelated controls case parents Control binary Case_Cont field:Phenotype parsing just bookkeeping. incorrect case/control diagnosis label can propagate DMP discovery, model training, performance assessment. Always inspect resulting labels simple tables continuing.","code":"\ngeo_ID <- \"GSE97362\"\npath_save <- file.path(project_root, \"Kabuki\", geo_ID)\npath_idat <- file.path(path_save, \"IDATs\")\nensure_project_dirs(path_save)\n\ngse <- GEOquery::getGEO(GEO = geo_ID, destdir = path_idat)[[1]]\n\npheno_gse97362 <- Biobase::pData(gse) %>%\n  dplyr::mutate(\n    Age = `age (years):ch1`,\n    Sex = dplyr::case_when(\n      `gender:ch1` == \"male\" ~ \"Male\",\n      `gender:ch1` == \"female\" ~ \"Female\",\n      TRUE ~ NA_character_\n    ),\n    Diagnosis = `disease state:ch1`,\n    SampleType = `sample type:ch1`,\n    Case_Cont = NA_character_\n  ) %>%\n  dplyr::select(title, geo_accession, Sex, Case_Cont, Age, Diagnosis, SampleType)\npheno_gse116300 <- Biobase::pData(gse) %>%\n  dplyr::mutate(\n    Sex = dplyr::case_when(\n      `Sex:ch1` == \"Male\" ~ \"Male\",\n      `Sex:ch1` == \"Female\" ~ \"Female\",\n      TRUE ~ NA_character_\n    ),\n    Diagnosis = `case status:ch1`,\n    Case_Cont = dplyr::case_when(\n      Diagnosis %in% c(\"control\", \"case parent\") ~ \"Control\",\n      TRUE ~ \"Case\"\n    ),\n    variant_classification = `variant classification:ch1`,\n    mutation = `mutation:ch1`\n  ) %>%\n  dplyr::select(title, geo_accession, Sex, Case_Cont, variant_classification, mutation)"},{"path":"setup-data.html","id":"download-raw-idats","chapter":"3 Setup and Data Organization","heading":"3.9 Download raw IDATs","text":"Many GEO methylation studies provide raw IDAT files supplementary archives. example shows download pattern used GSE119778:extraction, array sample matching *_Grn.idat *_Red.idat file. minfi::read.metharray.exp() reads paired red green channel files raw methylation object (Aryee et al. 2014).","code":"wget \"https://www.ncbi.nlm.nih.gov/geo/download/?acc=GSE119778&format=file\" -O GSE119778_RAW.tar\ntar -xf GSE119778_RAW.tar -C IDATs"},{"path":"preprocessing-qc.html","id":"preprocessing-qc","chapter":"4 Preprocessing and Quality Control","heading":"4 Preprocessing and Quality Control","text":"chapter explains main preprocessing quality-control steps used Williams Kabuki methylation-array datasets. goal show check data used DMP discovery classification.","code":""},{"path":"preprocessing-qc.html","id":"raw-object-rgchannelset","chapter":"4 Preprocessing and Quality Control","heading":"4.1 Raw object: RGChannelSet","text":"IDATs first read RGChannelSet, stores raw red green channel intensities normalization (Aryee et al. 2014). laboratory workflow, sample sheet can provide sample identifiers array positions. public GEO examples, script extracts GEO accession, slide, array position IDAT filenames instead.IDAT filename downloaded GEO starts GSM accession, followed slide array position. many local laboratory datasets, filenames contain slide array position:","code":"\nRGset <- minfi::read.metharray.exp(base = path_idat)\nRGset# GEO\nGSM3383551_9379086005_R01C01_Grn.idat\n\n# In-house arrays.\n9379086005_R01C01_Grn.idat"},{"path":"preprocessing-qc.html","id":"sample-detection-p-value-filter","chapter":"4 Preprocessing and Quality Control","heading":"4.2 Sample detection P-value filter","text":"Detection P-values measure whether probe signal can distinguished background. repository, first coarse sample-level filter calculates mean detection P-value across probes sample keeps samples mean 0.05.rule implemented scripts; universal methylation-array QC threshold. workflows may use fraction failed probes additional intensity-based metrics, exact rule reported results published.first sample-level check. Metadata matching, sex prediction, PCA reviewed later samples used discovery training.","code":"\ndetP <- minfi::detectionP(RGset)\nsample_detP <- colMeans(detP, na.rm = TRUE)\nprint(table(sample_detP < 0.05))\n\nRGset <- RGset[, sample_detP < 0.05]"},{"path":"preprocessing-qc.html","id":"attach-phenotype-metadata","chapter":"4 Preprocessing and Quality Control","heading":"4.3 Attach phenotype metadata","text":"examples read without sample sheet, phenotype table attached IDATs loaded. GSM accession extracted filename used key links array metadata.simple check confirms loaded samples received case/control label:","code":"\npheno <- readr::read_csv(\n  file.path(path_save, paste0(\"pheno_\", geo_ID, \".csv\")),\n  show_col_types = FALSE\n)\n\ncolData(RGset) <- data.frame(\n  geo_accession = sub(\"_.*\", \"\", colnames(RGset)),\n  idat_name = colnames(RGset),\n  stringsAsFactors = FALSE\n) %>%\n  dplyr::left_join(pheno, by = \"geo_accession\") %>%\n  tibble::column_to_rownames(\"geo_accession\") %>%\n  S4Vectors::DataFrame()\nstopifnot(!any(is.na(RGset$Case_Cont)))\ntable(RGset$Case_Cont, useNA = \"ifany\")"},{"path":"preprocessing-qc.html","id":"sex-prediction","chapter":"4 Preprocessing and Quality Control","heading":"4.4 Sex prediction","text":"Sex prediction provides additional sample-level QC check. disagreement reported methylation-predicted sex can indicate sample-label problem, metadata error, another issue deserves review. discordant result therefore flag investigate, automatic reason discard sample.reported sex unavailable, concordance checked. predicted value can still stored plotting , scientifically justified, used covariate later analyses.","code":"\nMSet <- minfi::preprocessNoob(RGset)\nMSet <- minfi::mapToGenome(MSet)\n\npredSex <- as.data.frame(minfi::getSex(MSet)) %>%\n  tibble::rownames_to_column(var = \"geo_accession\") %>%\n  dplyr::mutate(\n    predSex = dplyr::case_when(\n      predictedSex == \"M\" ~ \"Male\",\n      predictedSex == \"F\" ~ \"Female\",\n      TRUE ~ NA_character_\n    )\n  ) %>%\n  dplyr::select(geo_accession, predSex)\n\ncolData(RGset) <- as.data.frame(SummarizedExperiment::colData(RGset)) %>%\n  tibble::rownames_to_column(\"geo_accession\") %>%\n  dplyr::left_join(predSex, by = \"geo_accession\") %>%\n  dplyr::mutate(\n    Concordant_Sex = dplyr::case_when(\n      is.na(Sex) ~ NA_character_,\n      Sex == predSex ~ \"Concordant\",\n      TRUE ~ \"Discordant\"\n    )\n  ) %>%\n  tibble::column_to_rownames(\"geo_accession\") %>%\n  S4Vectors::DataFrame()\n\ntable(\"Reported\" = RGset$Sex, \"Predicted\" = RGset$predSex, useNA = \"ifany\")\ntable(RGset$Concordant_Sex, useNA = \"ifany\")"},{"path":"preprocessing-qc.html","id":"normalization","chapter":"4 Preprocessing and Quality Control","heading":"4.5 Normalization","text":"Williams discovery workflow uses functional normalization, method developed Illumina methylation arrays (Fortin et al. 2014):Kabuki datasets processed exactly way:GSE97362 GSE218186 use process_meth = \"funnorm\".GSE116300 uses process_meth = \"quantile\" also compared processed beta-values distributed GEO.difference important rather incidental. Preprocessing can shift methylation distributions, model assumed transfer unchanged across preprocessing methods platforms. tutorial, differences kept visible Kabuki analysis explicitly tests robustness across independent datasets technologies.","code":"\nGRset <- minfi::preprocessFunnorm(RGset, sex = RGset$Sex)"},{"path":"preprocessing-qc.html","id":"probe-filters","chapter":"4 Preprocessing and Quality Control","heading":"4.6 Probe filters","text":"normalization, scripts apply three probe-level filters:remove probes overlapping common SNPs dropLociWithSnps(maf = 0.01);remove known cross-reactive probes maxprobes::dropXreactiveLoci();keep probes detection P-value 0.01 every sample strict version workflow.filters reduce measurements likely unreliable difficult interpret.Requiring detection every sample conservative. minimizes missing values can remove many CpGs cohort size grows. tutorial helper can instead use 95% detected-sample rule small amount missingness acceptable. Whichever rule used fixed downstream analysis reported clearly.","code":"\nGRset <- minfi::dropLociWithSnps(GRset, maf = 0.01)\nGRset <- maxprobes::dropXreactiveLoci(GRset)\n\ndetP <- detP[rownames(GRset), GRset$idat_name, drop = FALSE]\nkeep <- rowSums(detP < 0.01, na.rm = TRUE) == ncol(GRset)\nprint(table(\"CpGs kept\" = keep))\n\nGRset <- GRset[keep, ]\nGRset$Dataset <- geo_ID"},{"path":"preprocessing-qc.html","id":"wrapper-function","chapter":"4 Preprocessing and Quality Control","heading":"4.7 Wrapper function","text":"new dataset, wrapper collects preprocessing choices one function returns normalized GenomicRatioSet downstream analysis. makes easier see settings used cohort.","code":"\nGRset <- read_idat_minfi(\n  path_idat = path_idat,\n  pheno = pheno,\n  path_save = path_save,\n  estimate_cell_counts = TRUE,\n  dataset_name = geo_ID,\n  process_meth = \"funnorm\"\n)\n\nplot_custom_theme <- function() {\n  theme_classic() +\n    theme(\n      legend.position = \"right\",\n      axis.text = element_text(size = 13),\n      axis.title = element_text(size = 15, face = \"bold\"),\n      legend.text = element_text(size = 10),\n      legend.title = element_text(size = 12, face = \"bold\"),\n      plot.title = element_text(size = 17, face = \"bold\", hjust = 0.5)\n    )\n}\n\nsaveRDS(GRset, file.path(path_save, paste0(\"GRset_\", geo_ID, \".rds\")))"},{"path":"preprocessing-qc.html","id":"cell-composition","chapter":"4 Preprocessing and Quality Control","heading":"4.8 Cell composition","text":"Whole-blood DNA contains mixture leukocyte populations, cell type characteristic methylation profile. Differences cell composition cases controls can therefore create apparent methylation differences directly related disorder.tutorial uses FlowSorted.Blood.EPIC estimate major immune-cell fractions raw RGChannelSet (Salas et al. 2018). estimates later included covariates Williams differential methylation model. Cell deconvolution one way reduce confounding; make samples biologically identical.","code":"\nRGset <- minfi::read.metharray.exp(base = path_idat) # Reads the IDATs.\n\nlibrary(FlowSorted.Blood.EPIC)\n\ncell_estimates <- estimateCellCounts2(\n  RGset, compositeCellType = \"Blood\",\n  processMethod = \"preprocessNoob\",\n  probeSelect = \"IDOL\",\n  cellTypes = c(\"CD8T\", \"CD4T\", \"NK\",\n                \"Bcell\",\"Mono\", \"Neu\")\n)\n\nas.data.frame(cell_estimates$prop) %>%\n  rownames_to_column(var = \"geo_accession\") %>%\n  mutate(\n    \"geo_accession\" = sub(\"_.*\", \"\", geo_accession)\n  ) %>%\n  readr::write_csv(file = paste0(path_save, \"Tables/CellCounts_\", geo_ID, \".csv\"))"},{"path":"preprocessing-qc.html","id":"pca-for-batch-effects","chapter":"4 Preprocessing and Quality Control","heading":"4.9 PCA for batch effects","text":"testing differential methylation, workflow uses PCA inspect largest sources variation. QC view, variability calculated 10,000 variable autosomal CpGs among controls, resulting PCA inspected slide, array position, sex, case/control status. purpose identify possible technical structure unusual samples feature discovery.PCA coordinates can coloured slide, array, case/control status see whether one variables dominates first principal components:","code":"\nGRset$Slide <- stringr::str_split(GRset$idat_name, \"_\") %>%\n  purrr::map_chr(~ .x[2]) %>%\n  factor()\n\nGRset$Array <- stringr::str_split(GRset$idat_name, \"_\") %>%\n  purrr::map_chr(~ .x[3]) %>%\n  factor()\n\nannots <- if (exists(\"array_annots\", mode = \"function\")) {\n  array_annots(GRset)\n} else {\n  minfi::getAnnotation(GRset)\n}\nrowData(GRset) <- annots[rownames(GRset), ]\n\nGRset_noXY <- GRset[!SummarizedExperiment::rowData(GRset)$chr %in% c(\"chrX\", \"chrY\"), ]\n\nvariance_m <- apply(\n  minfi::getM(GRset_noXY[, GRset_noXY$Case_Cont == \"Control\"]),\n  1,\n  stats::var\n)\ntop_var <- sort(variance_m, decreasing = TRUE)[1:10000]\n\npca_res <- stats::prcomp(\n  t(minfi::getBeta(GRset_noXY[names(top_var), ])),\n  scale. = TRUE\n)\n\nvar_explained <- round(100 * pca_res$sdev^2 / sum(pca_res$sdev^2), 2)\n\npca_df <- as.data.frame(pca_res$x) %>%\n  dplyr::select(PC1, PC2) %>%\n  tibble::rownames_to_column(\"geo_accession\") %>%\n  dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_noXY)), by = \"geo_accession\")\npca_panels <- lapply(c(\"Slide\", \"Array\", \"Case_Cont\"), function(col_var) {\n  ggplot2::ggplot(pca_df, ggplot2::aes(PC1, PC2)) +\n    ggplot2::stat_ellipse(\n      ggplot2::aes(color = .data[[col_var]]),\n      level = 0.95,\n      show.legend = FALSE\n    ) +\n    ggplot2::geom_point(ggplot2::aes(color = .data[[col_var]], shape = Sex), size = 2.5) +\n    ggplot2::labs(\n      title = paste(\"PCA plot -\", col_var),\n      x = paste0(\"PC1 (\", var_explained[1], \"% var)\"),\n      y = paste0(\"PC2 (\", var_explained[2], \"% var)\")\n    ) +\n    plot_custom_theme()\n})\n\nggplot2::ggsave(\n  file.path(path_figures, paste0(\"PCA_\", geo_ID, \".png\")),\n  ggpubr::ggarrange(plotlist = pca_panels, ncol = 3),\n  width = 13,\n  height = 5,\n  bg = \"white\"\n)"},{"path":"preprocessing-qc.html","id":"pca-outliers-and-held-out-validation","chapter":"4 Preprocessing and Quality Control","heading":"4.10 PCA outliers and held-out validation","text":"Williams example, script uses PC1/PC2 coordinates flag samples outside group-specific PCA distribution also reserves five non-outlier samples per case/control group hold-outs. Together, reviewed outliers random hold-outs form within-dataset validation set used later tutorial.PCA outlier flags prompts review, automatic exclusion rules. removing sample, check phenotype, array position, signal quality, sex prediction, available technical information. especially important rare-disease cohorts, biologically unusual patient may also scientifically informative.","code":"\noutliers_df <- detect_pca_outliers(pca_df, return_all = TRUE)\n\noutliers_df %>%\n  dplyr::filter(outside) %>%\n  dplyr::select(geo_accession, Case_Cont, D2, outside) %>%\n  openxlsx::write.xlsx(\n    file.path(path_save, \"Tables\", paste0(\"PCA_outliers_\", geo_ID, \".xlsx\")),\n    rowNames = FALSE\n  )\n\nset.seed(42)\noutliers_df %>%\n  dplyr::filter(!outside) %>%\n  dplyr::group_by(Case_Cont) %>%\n  dplyr::slice_sample(n = min(5, dplyr::n())) %>%\n  dplyr::select(geo_accession, Case_Cont, D2, outside) %>%\n  openxlsx::write.xlsx(\n    file.path(path_save, \"Tables\", paste0(\"KeepOut_Validation_\", geo_ID, \".xlsx\")),\n    rowNames = FALSE\n  )"},{"path":"williams.html","id":"williams","chapter":"5 Williams Syndrome Workflow","heading":"5 Williams Syndrome Workflow","text":"Williams analysis de novo episignature-discovery example used manuscript. starts public blood methylation-array data, identifies case-control differentially methylated positions (DMPs), reduces DMPs smaller CpG panel, trains classifiers, asks whether selected pattern remains detectable samples used discovery.GSE119778 discovery dataset Kimura et al. (Kimura et al. 2020; National Center Biotechnology Information 2026b). GSE66552, Strong et al., provides independent external cohort containing typical controls, Williams syndrome samples, 7q11.23 duplication samples (Strong et al. 2015; National Center Biotechnology Information 2026d).","code":""},{"path":"williams.html","id":"workflow-overview","chapter":"5 Williams Syndrome Workflow","heading":"5.1 Workflow overview","text":"main script scripts/Williams_analysis.R. Age available GSE119778 metadata used workflow, age added differential methylation model. model instead adjusts available sex information estimated blood-cell proportions. illustrates important rule small rare-disease datasets: use covariates available, scientifically justified, supported sample size; add variables simply commonly used elsewhere.manuscript analysis, GSE119778 contains 34 Williams syndrome samples 34 controls discovery/validation split. Seven cases seven controls ultimately reserved within-dataset validation, leaving 27 cases 27 controls discovery. external GSE66552 cohort contains 20 Williams syndrome samples, 15 typical controls, 10 7q11.23 duplication samples.conceptual flow :Prepare GSE119778 phenotype IDAT data.Normalize, filter, save GRset_GSE119778.rds.Merge estimated blood cell proportions.Run PCA inspect batch effects select outliers/hold-outs.Define discovery set excluding PCA outliers validation hold-outs.Run limma M-values find DMPs.Rank prune DMPs create episignature CpG panel.Train classifiers discovery set.Score discovery, internal validation, GSE66552 external samples.","code":""},{"path":"williams.html","id":"prepare-the-discovery-object","chapter":"5 Williams Syndrome Workflow","heading":"5.2 Prepare the discovery object","text":"preprocessing steps Chapter 4, reload normalized methylation object attach estimated blood-cell proportions used covariates DMP model.later limma model includes CD8T, CD4T, NK, Bcell, Mono, Neu estimates. columns therefore need attached sample metadata design matrix created.","code":"\ngeo_ID <- \"GSE119778\"\npath_save <- file.path(project_root, \"Williams\", geo_ID)\npath_figures <- file.path(path_save, \"Figures\")\n\nGRset <- readRDS(file.path(path_save, paste0(\"GRset_\", geo_ID, \".rds\")))\nGRset$Case_Cont <- factor(GRset$Case_Cont, levels = c(\"Control\", \"Case\"))\n\ncell_counts <- readr::read_csv(\n  file.path(path_save, \"Tables\", paste0(\"CellCounts_\", geo_ID, \".csv\")),\n  show_col_types = FALSE\n)\n\ncolData(GRset) <- as.data.frame(SummarizedExperiment::colData(GRset)) %>%\n  dplyr::left_join(cell_counts, by = \"geo_accession\") %>%\n  tibble::column_to_rownames(\"geo_accession\") %>%\n  S4Vectors::DataFrame()"},{"path":"williams.html","id":"define-discovery-and-validation-samples","chapter":"5 Williams Syndrome Workflow","heading":"5.3 Define discovery and validation samples","text":"PCA/QC step writes two tables:PCA_outliers_GSE119778.xlsx, containing samples flagged review PCA;KeepOut_Validation_GSE119778.xlsx, containing randomly selected non-outlier hold-outs.Neither group used DMP discovery CpG selection. important validation samples remain independent feature discovery (Ambroise McLachlan 2002).Remove sex chromosome probes DMP testing:Removing X- Y-chromosome probes reduces chance sex-associated methylation differences dominate case-control DMP results. Reported/predicted sex still included sample-level covariate model.","code":"\noutlier_smpl <- readxl::read_xlsx(\n  file.path(path_save, \"Tables\", paste0(\"PCA_outliers_\", geo_ID, \".xlsx\"))\n) %>%\n  dplyr::pull(geo_accession)\n\nvalidation_smpl <- readxl::read_xlsx(\n  file.path(path_save, \"Tables\", paste0(\"KeepOut_Validation_\", geo_ID, \".xlsx\"))\n) %>%\n  dplyr::pull(geo_accession)\n\nGRset_Discovery <- GRset[, !colnames(GRset) %in% c(outlier_smpl, validation_smpl)]\nannots <- minfi::getAnnotation(GRset_Discovery)\nstopifnot(identical(rownames(annots), rownames(GRset_Discovery)))\n\nGRset_Discovery <- GRset_Discovery[!annots$chr %in% c(\"chrX\", \"chrY\"), ]"},{"path":"williams.html","id":"differential-methylation-with-limma","chapter":"5 Williams Syndrome Workflow","heading":"5.4 Differential methylation with limma","text":"Williams script fits limma model M-values, case/control status main term sex plus estimated blood-cell fractions covariates (Ritchie et al. 2015).coefficient Case_ContCase means contrast Case minus Control, Case_Cont defined Control reference level.","code":"\ndesign <- stats::model.matrix(\n  ~ Case_Cont + Sex + CD8T + CD4T + NK + Bcell + Mono + Neu,\n  data = as.data.frame(SummarizedExperiment::colData(GRset_Discovery))\n)\n\nfit <- limma::lmFit(object = minfi::getM(GRset_Discovery), design = design)\nfit <- limma::eBayes(fit = fit)\ntt <- limma::topTable(fit, coef = \"Case_ContCase\", number = Inf)"},{"path":"williams.html","id":"add-interpretable-effect-sizes","chapter":"5 Williams Syndrome Workflow","heading":"5.5 Add interpretable effect sizes","text":"limma P-values complemented effect size calculated beta-values. CpG, script calculates:positive deltaBeta means average beta-value higher Williams syndrome samples controls; negative value means lower. M-values used statistical model, whereas beta-values make size direction methylation difference easier interpret (Du et al. 2010).","code":"deltaBeta = mean(beta in cases) - mean(beta in controls)\ndeltaBeta <- as.data.frame(SummarizedExperiment::assay(GRset_Discovery, \"Beta\")) %>%\n  tibble::rownames_to_column(var = \"id\") %>%\n  tidyr::pivot_longer(cols = -id, names_to = \"geo_accession\", values_to = \"Beta\") %>%\n  dplyr::left_join(\n    as.data.frame(SummarizedExperiment::colData(GRset_Discovery)) %>%\n      dplyr::select(geo_accession, Case_Cont),\n    by = \"geo_accession\"\n  ) %>%\n  dplyr::group_by(id) %>%\n  dplyr::reframe(\n    deltaBeta = mean(Beta[Case_Cont == \"Case\"]) - mean(Beta[Case_Cont == \"Control\"])\n  )\n\ntt_annot <- tt %>%\n  tibble::rownames_to_column(var = \"id\") %>%\n  dplyr::left_join(deltaBeta, by = \"id\") %>%\n  dplyr::select(id, P.Value, adj.P.Val, deltaBeta) %>%\n  dplyr::left_join(\n    as.data.frame(annots) %>%\n      dplyr::select(Name, chr, pos, Relation_to_Island, UCSC_RefGene_Name),\n    by = c(\"id\" = \"Name\")\n  )\n\nopenxlsx::write.xlsx(\n  tt_annot,\n  file.path(path_save, \"Tables\", paste0(\"DMPs_limma_\", geo_ID, \".xlsx\")),\n  rowNames = FALSE\n)"},{"path":"williams.html","id":"volcano-plot","chapter":"5 Williams Syndrome Workflow","heading":"5.6 Volcano plot","text":"Williams example, manuscript tutorial use two filters:adjusted P-value (adj.P.Val) < 0.01;absolute methylation difference (abs(deltaBeta)) > 0.10.analysis choices example, universal episignature thresholds. manuscript analysis, identify 1,681 hypermethylated 1,082 hypomethylated DMPs additional feature-selection steps.","code":"\nthresholds_DMPs <- list(adj.P.Val = 0.01, deltaBeta = 0.1)\n\nplt_volcano <- tt_annot %>%\n  dplyr::mutate(\n    logP = -log10(adj.P.Val),\n    sign = factor(\n      dplyr::case_when(\n        adj.P.Val < thresholds_DMPs[[\"adj.P.Val\"]] &\n          deltaBeta > thresholds_DMPs[[\"deltaBeta\"]] ~ \"HyperM\",\n        adj.P.Val < thresholds_DMPs[[\"adj.P.Val\"]] &\n          deltaBeta < -thresholds_DMPs[[\"deltaBeta\"]] ~ \"HypoM\",\n        TRUE ~ \"NS\"\n      ),\n      levels = c(\"HypoM\", \"NS\", \"HyperM\")\n    )\n  ) %>%\n  ggplot2::ggplot(ggplot2::aes(deltaBeta, logP, color = sign)) +\n  ggplot2::geom_point(alpha = 0.2, size = 2) +\n  ggplot2::geom_vline(xintercept = c(-0.1, 0.1), lty = 2, color = \"gray\") +\n  ggplot2::geom_hline(yintercept = -log10(0.01), lty = 2, color = \"gray\") +\n  ggplot2::scale_color_manual(values = c(NS = \"gray\", HyperM = \"#c01313\", HypoM = \"#3939e0\")) +\n  ggplot2::labs(x = \"Delta beta (Case - Control)\", y = \"-log10(FDR)\", color = \"\") +\n  plot_custom_theme()\n\nggplot2::ggsave(\n  file.path(path_figures, paste0(\"Volcano_plt\", geo_ID, \".png\")),\n  plt_volcano,\n  width = 5,\n  height = 4,\n  bg = \"white\"\n)"},{"path":"williams.html","id":"select-episignature-cpgs","chapter":"5 Williams Syndrome Workflow","heading":"5.7 Select episignature CpGs","text":"next steps reduce significant DMPs smaller set candidate classifier features. decisions made discovery samples .","code":""},{"path":"williams.html","id":"step-1-rank-significant-dmps","chapter":"5 Williams Syndrome Workflow","heading":"5.7.1 Step 1: rank significant DMPs","text":"combined score prioritizes CpGs strong statistical evidence large case-control methylation difference. ranking device; show individual CpG known biological function.","code":"\ntt_flt <- tt_annot %>%\n  dplyr::filter(\n    adj.P.Val < thresholds_DMPs[[\"adj.P.Val\"]],\n    abs(deltaBeta) > thresholds_DMPs[[\"deltaBeta\"]]\n  ) %>%\n  dplyr::mutate(Pval_deltaBeta = -log10(adj.P.Val) * abs(deltaBeta)) %>%\n  dplyr::arrange(dplyr::desc(Pval_deltaBeta)) %>%\n  dplyr::slice_head(n = 1000)"},{"path":"williams.html","id":"step-2-remove-redundant-cpgs","chapter":"5 Williams Syndrome Workflow","heading":"5.7.2 Step 2: remove redundant CpGs","text":"Highly correlated CpGs carry overlapping information. Removing redundancy reduces number predictors helps prevent final panel dominated groups CpGs similar behaviour, whether CpGs physically close genome.","code":"\nbeta_values <- minfi::getBeta(GRset_Discovery[tt_flt$id, ])\n\nrm_CpG <- caret::findCorrelation(\n  t(beta_values),\n  cutoff = 0.9,\n  verbose = TRUE,\n  names = TRUE\n)"},{"path":"williams.html","id":"step-3-keep-high-importance-cpgs","chapter":"5 Williams Syndrome Workflow","heading":"5.7.3 Step 3: keep high-importance CpGs","text":"nonpara = TRUE, caret::filterVarImp() evaluates CpG individually using two-class ROC-based measure discrimination (Kuhn 2008). script keeps CpGs importance median, corresponding informative half remaining features.manuscript analysis, filtering sequence reduces top 1,000 ranked DMPs 763 CpGs correlation pruning 374 CpGs variable-importance filter. 374 CpGs form candidate Williams episignature panel used later PCA classifier training.","code":"\nvar_Imp <- caret::filterVarImp(\n  x = as.data.frame(t(beta_values[!rownames(beta_values) %in% rm_CpG, ])),\n  y = GRset_Discovery$Case_Cont,\n  nonpara = TRUE\n) %>%\n  tibble::rownames_to_column(var = \"id\") %>%\n  dplyr::arrange(dplyr::desc(Case)) %>%\n  dplyr::filter(Case > stats::quantile(Case, 0.5)) %>%\n  dplyr::select(id, Case) %>%\n  dplyr::rename(Importance = Case)\n\ntt_flt_Imp <- tt_flt %>%\n  dplyr::left_join(var_Imp, by = \"id\") %>%\n  dplyr::filter(!is.na(Importance))\n\nopenxlsx::write.xlsx(\n  tt_flt_Imp,\n  file.path(path_save, \"Tables\", paste0(\"EpisignatureCpGs_\", geo_ID, \".xlsx\")),\n  rowNames = FALSE\n)"},{"path":"williams.html","id":"visualize-the-selected-cpgs","chapter":"5 Williams Syndrome Workflow","heading":"5.8 Visualize the selected CpGs","text":"selected CpGs first inspected unsupervised PCA. PCA use case/control labels create axes, provides simple visual check whether selected methylation pattern separates groups held-samples fall. visualization supports classifier analysis substitute independent prediction.","code":"\npca_res <- stats::prcomp(\n  t(minfi::getBeta(GRset[tt_flt_Imp$id, ])),\n  scale. = TRUE,\n  center = TRUE\n)\n\nvar_explained <- round(100 * pca_res$sdev^2 / sum(pca_res$sdev^2), 2)\n\npca_df <- as.data.frame(pca_res$x) %>%\n  dplyr::select(PC1, PC2) %>%\n  tibble::rownames_to_column(var = \"geo_accession\") %>%\n  dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset)), by = \"geo_accession\") %>%\n  dplyr::mutate(\n    Status = dplyr::case_when(\n      !geo_accession %in% c(outlier_smpl, validation_smpl) & Case_Cont == \"Case\" ~ \"Williams_D\",\n      !geo_accession %in% c(outlier_smpl, validation_smpl) & Case_Cont == \"Control\" ~ \"Control_D\",\n      geo_accession %in% c(outlier_smpl, validation_smpl) & Case_Cont == \"Case\" ~ \"Williams_V\",\n      geo_accession %in% c(outlier_smpl, validation_smpl) & Case_Cont == \"Control\" ~ \"Control_V\",\n      TRUE ~ \"Other\"\n    )\n  )\n\nggplot2::ggplot(pca_df, ggplot2::aes(PC1, PC2, color = Status, shape = Sex)) +\n  ggplot2::geom_point(size = 2.5, alpha = 0.8) +\n  ggplot2::labs(\n    title = \"Williams episignature\",\n    x = paste0(\"PC1 (\", var_explained[1], \"% var)\"),\n    y = paste0(\"PC2 (\", var_explained[2], \"% var)\")\n  ) +\n  plot_custom_theme()"},{"path":"williams.html","id":"train-classifiers","chapter":"5 Williams Syndrome Workflow","heading":"5.9 Train classifiers","text":"original Williams script compares several classifiers trains available _training_f wrappers using beta-values M-values. Depending installed packages, include SVM, penalized/elastic-net models, random forest, KNN, PLS, naive Bayes, gradient boosting, XGBoost, neural-network models.first pass tutorial, linear SVM simplest model follow also consistent classifier family commonly used episignature studies. selected CpG panel fixed model training validation samples scored:reproduce broader model sweep:larger model sweep useful comparing algorithms, identical performance strong Williams example interpreted evidence algorithms interchangeable. manuscript analysis reports correct classification available Williams syndrome control samples across evaluated models internal external validation; result reflects strength particular methylation signal generalized disorders.","code":"\nsvm_mod <- svm_training_f(\n  GRset_sbst = GRset_Discovery[tt_flt_Imp$id, ],\n  repeats = 10,\n  number = 5,\n  sampling = NULL,\n  beta_M = \"Beta\",\n  n_cores = 5\n)\n\nsaveRDS(svm_mod, file.path(path_save, paste0(\"svm_models_\", geo_ID, \".rds\")))\nmodel_funs <- grep(\"_training_f$\", ls(), value = TRUE)\n\nmods_beta <- purrr::set_names(model_funs) %>%\n  purrr::map(function(fun_name) {\n    fun <- get(fun_name)\n    res <- fun(\n      GRset_sbst = GRset_Discovery[tt_flt_Imp$id, ],\n      repeats = 10,\n      number = 5,\n      sampling = NULL,\n      beta_M = \"Beta\"\n    )\n    res$algorithm_name <- fun_name\n    res$beta_M <- \"Beta\"\n    res\n  })\n\nmods_m <- purrr::set_names(model_funs) %>%\n  purrr::map(function(fun_name) {\n    fun <- get(fun_name)\n    res <- fun(\n      GRset_sbst = GRset_Discovery[tt_flt_Imp$id, ],\n      repeats = 10,\n      number = 5,\n      sampling = NULL,\n      beta_M = \"M\"\n    )\n    res$algorithm_name <- fun_name\n    res$beta_M <- \"M\"\n    res\n  })\n\nmods <- c(\n  stats::setNames(mods_beta, paste0(names(mods_beta), \"_Beta\")),\n  stats::setNames(mods_m, paste0(names(mods_m), \"_M\"))\n)\n\nsaveRDS(mods, file.path(path_save, paste0(\"_classification_models_\", geo_ID, \".rds\")))"},{"path":"williams.html","id":"score-discovery-and-validation-samples","chapter":"5 Williams Syndrome Workflow","heading":"5.10 Score discovery and validation samples","text":"list models:","code":"\nmods <- readRDS(file.path(path_save, paste0(\"_classification_models_\", geo_ID, \".rds\")))\n\nGRset_Discovery <- GRset[, !colnames(GRset) %in% c(outlier_smpl, validation_smpl)]\nGRset_Validation <- GRset[, !colnames(GRset) %in% colnames(GRset_Discovery)]\n\npred_discovery <- lapply(mods, function(spec_mod) {\n  pred <- predict_model_on_grset(spec_mod, GRset_Discovery)\n  pred %>%\n    dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_Discovery)), by = \"geo_accession\") %>%\n    dplyr::mutate(\n      Status = dplyr::if_else(Case_Cont == \"Case\", \"Williams_D\", \"Control_D\"),\n      Dataset = geo_ID,\n      Algorithm = spec_mod$algorithm_name,\n      beta_M = spec_mod$beta_M\n    ) %>%\n    dplyr::select(geo_accession, Status, Case, Dataset, Algorithm, beta_M)\n})\n\npred_validation <- lapply(mods, function(spec_mod) {\n  pred <- predict_model_on_grset(spec_mod, GRset_Validation)\n  pred %>%\n    dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_Validation)), by = \"geo_accession\") %>%\n    dplyr::mutate(\n      Status = dplyr::if_else(Case_Cont == \"Case\", \"Williams_V\", \"Control_V\"),\n      Dataset = geo_ID,\n      Algorithm = spec_mod$algorithm_name,\n      beta_M = spec_mod$beta_M\n    ) %>%\n    dplyr::select(geo_accession, Status, Case, Dataset, Algorithm, beta_M)\n})"},{"path":"williams.html","id":"prepare-and-score-gse66552","chapter":"5 Williams Syndrome Workflow","heading":"5.11 Prepare and score GSE66552","text":"independent GSE66552 preparation script creates:object restricted Williams CpG panel selected GSE119778. external samples used choose CpGs fit classifiers.Combine predictions:","code":"Williams/GSE66552/GRset_epi_GSE66552.rds\ngeo_ID_external <- \"GSE66552\"\nGRset_external <- readRDS(\n  file.path(project_root, \"Williams\", geo_ID_external, paste0(\"GRset_epi_\", geo_ID_external, \".rds\"))\n)\n\npred_external <- lapply(mods, function(spec_mod) {\n  pred <- predict_model_on_grset(spec_mod, GRset_external)\n  pred %>%\n    dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_external)), by = \"geo_accession\") %>%\n    dplyr::mutate(\n      Status = dplyr::case_when(\n        Diagnosis == \"WS\" ~ \"Williams_E\",\n        Diagnosis == \"TD control\" ~ \"Control_E\",\n        Diagnosis == \"Dup7\" ~ \"Dup7_E\",\n        TRUE ~ NA_character_\n      ),\n      Dataset = geo_ID_external,\n      Algorithm = spec_mod$algorithm_name,\n      beta_M = spec_mod$beta_M\n    ) %>%\n    dplyr::select(geo_accession, Status, Case, Dataset, Algorithm, beta_M)\n})\npredictions_df <- dplyr::bind_rows(\n  dplyr::bind_rows(pred_discovery),\n  dplyr::bind_rows(pred_validation),\n  dplyr::bind_rows(pred_external)\n) %>%\n  dplyr::mutate(\n    Status = factor(\n      Status,\n      levels = c(\"Control_D\", \"Williams_D\", \"Control_V\", \"Williams_V\", \"Control_E\", \"Williams_E\", \"Dup7_E\")\n    ),\n    dataset_id = factor(Dataset, levels = c(\"GSE119778\", \"GSE66552\"))\n  )\n\nopenxlsx::write.xlsx(\n  predictions_df,\n  file.path(path_save, \"Tables\", paste0(\"Classification_predictions_\", geo_ID, \".xlsx\")),\n  rowNames = FALSE\n)"},{"path":"williams.html","id":"interpret-the-williams-outputs","chapter":"5 Williams Syndrome Workflow","heading":"5.12 Interpret the Williams outputs","text":"reviewing final scores, look following pattern:discovery controls score toward control end discovery Williams samples toward case end;held-samples behave similarly expected group;independent Williams samples retain disease-associated signal;independent typical controls remain control-like;7q11.23 duplication samples provide additional comparator simply reproduce Williams deletion profile.Warning signs include whole external dataset shifting together, much poorer performance held-samples discovery samples, scores track technical variable slide array, missing model CpGs external dataset.main lesson Williams workflow model fitting. moves order discovery, feature selection, locked model training, finally independent testing. separation allows final result address whether methylation pattern generalizes beyond samples created .","code":""},{"path":"kabuki.html","id":"kabuki","chapter":"6 Kabuki Workflow","heading":"6 Kabuki Workflow","text":"Kabuki workflow existing-signature validation cross-platform example used manuscript. Unlike Williams chapter, discover new CpG panel limma. Instead, starts established Kabuki methylation signature tests whether disease-associated signal remains detectable across independent array cohorts long-read methylation data (Aref-Eshghi et al. 2017, 2020).CpG list used manuscript contains 150 published sites. One site (cg22948959) available datasets, cross-dataset analysis uses shared set available across compared data. CpG availability must checked trained model applied.core scripts :scripts/Kabuki/GSE97362/Kabuki_GSE97362_analysis.Rscripts/Kabuki/GSE116300/Kabuki_GSE116300_analysis.Rscripts/Kabuki/GSE218186/Kabuki_GSE218186_analysis.Rscripts/Kabuki/PMID41225292/LR_analysis.Rscripts/Kabuki/Kabuki_analysis.RA central lesson successful classification technical equivalence. array-trained classifier can preserve strong Kabuki disease signal even numerical methylation values arrays, ONT, PacBio different distributions. PCA, clustering, later deviation analysis therefore used examine platform effects separately disease classification (Hildonen et al. 2026).","code":""},{"path":"kabuki.html","id":"kabuki-data-sources","chapter":"6 Kabuki Workflow","heading":"6.1 Kabuki data sources","text":"fixed Kabuki CpG list used tutorial stored :","code":"scripts/Kabuki/PMID41225292/episignature_PMID41225292.xlsx"},{"path":"kabuki.html","id":"prepare-the-gse97362-training-cohort","chapter":"6 Kabuki Workflow","heading":"6.2 Prepare the GSE97362 training cohort","text":"GSE97362 script first harmonizes phenotype metadata original Kabuki/CHARGE study (Butcher et al. 2017).preprocesses IDATs:","code":"\ngeo_ID <- \"GSE97362\"\npath_save <- file.path(project_root, \"Kabuki\", geo_ID)\npath_idat <- file.path(path_save, \"IDATs\")\npath_figures <- file.path(path_save, \"Figures\")\nensure_project_dirs(path_save)\n\ngse <- GEOquery::getGEO(GEO = geo_ID, destdir = path_idat)[[1]]\n\npheno <- Biobase::pData(gse) %>%\n  dplyr::mutate(\n    Age = `age (years):ch1`,\n    Sex = dplyr::case_when(\n      `gender:ch1` == \"male\" ~ \"Male\",\n      `gender:ch1` == \"female\" ~ \"Female\",\n      TRUE ~ NA_character_\n    ),\n    Diagnosis = `disease state:ch1`,\n    SampleType = `sample type:ch1`,\n    Case_Cont = NA_character_\n  ) %>%\n  dplyr::select(title, geo_accession, Sex, Case_Cont, Age, Diagnosis, SampleType)\n\nreadr::write_csv(pheno, file.path(path_save, paste0(\"pheno_\", geo_ID, \".csv\")))\nGRset <- read_idat_minfi(\n  path_idat = path_idat,\n  pheno = readr::read_csv(file.path(path_save, paste0(\"pheno_\", geo_ID, \".csv\")), show_col_types = FALSE),\n  path_save = path_save,\n  estimate_cell_counts = FALSE,\n  dataset_name = geo_ID,\n  process_meth = \"funnorm\"\n)\n\nsaveRDS(GRset, file.path(path_save, paste0(\"GRset_\", geo_ID, \".rds\")))"},{"path":"kabuki.html","id":"subset-to-published-kabuki-cpgs","chapter":"6 Kabuki Workflow","heading":"6.3 Subset to published Kabuki CpGs","text":"harmonized colData keeps detailed publication labels Diagnosis SampleType, also creating binary Case_Cont field needed classifier. detailed labels remain important every sample dataset part training set.","code":"\nepisignature_cpgs <- openxlsx::read.xlsx(\n  file.path(project_root, \"Kabuki\", \"PMID41225292\", \"episignature_PMID41225292.xlsx\")\n)\n\nGRset_epis <- GRset[rownames(GRset) %in% episignature_cpgs$id, ]\ncolData(GRset_epis) <- as.data.frame(SummarizedExperiment::colData(GRset_epis)) %>%\n  dplyr::mutate(\n    Case_Cont = factor(\n      dplyr::case_when(\n        Diagnosis == \"Control\" ~ \"Control\",\n        TRUE ~ \"Case\"\n      ),\n      levels = c(\"Case\", \"Control\")\n    )\n  ) %>%\n  dplyr::select(title, geo_accession, Case_Cont, Sex, Age, Diagnosis, Dataset, SampleType) %>%\n  S4Vectors::DataFrame()\n\nsaveRDS(GRset_epis, file.path(path_save, paste0(\"GRset_epis_\", geo_ID, \".rds\")))"},{"path":"kabuki.html","id":"train-the-kabuki-svm","chapter":"6 Kabuki Workflow","heading":"6.4 Train the Kabuki SVM","text":"SVM trained molecularly confirmed KMT2D loss--function discovery subset designated controls. Kabuki-related, CHARGE/CHD7, variant samples kept outside training subset can evaluated afterward.model returns Case probability score. Higher values mean sample similar Kabuki class learned training data; lower values control-like. Williams example, model-derived score rather universal clinical cutoff.","code":"\nGRset_train <- GRset_epis[\n  ,\n  GRset_epis$SampleType %in% c(\n    \"Control for KMT2D LOF discovery cohort\",\n    \"KMT2D LOF discovery cohort\",\n    \"Control for validation cohort\"\n  )\n]\n\nGRset_train$Case_Cont <- factor(\n  dplyr::case_when(\n    GRset_train$SampleType == \"Control for KMT2D LOF discovery cohort\" ~ \"Control\",\n    GRset_train$SampleType == \"KMT2D LOF discovery cohort\" ~ \"Case\",\n    GRset_train$SampleType == \"Control for validation cohort\" ~ \"Control\",\n    TRUE ~ NA_character_\n  ),\n  levels = c(\"Case\", \"Control\")\n)\n\nsvm_mod <- svm_training_f(GRset_sbst = GRset_train, beta_M = \"Beta\")\n\nsaveRDS(svm_mod, file.path(path_save, paste0(\"svm_model_\", geo_ID, \".rds\")))"},{"path":"kabuki.html","id":"score-all-gse97362-samples","chapter":"6 Kabuki Workflow","heading":"6.5 Score all GSE97362 samples","text":"CHARGE/CHD7 samples provide useful cross-disease specificity check CHARGE Kabuki can show overlapping developmental phenotypes distinct methylation signatures (Butcher et al. 2017). classifier separates Kabuki healthy controls weaker test one also challenged related disorder.","code":"\nsvm_mod <- readRDS(file.path(path_save, paste0(\"svm_model_\", geo_ID, \".rds\")))\n\npredictions_df <- predict_model_on_grset(svm_mod, GRset_epis) %>%\n  dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_epis)), by = \"geo_accession\") %>%\n  dplyr::mutate(\n    DiscVal = factor(\n      dplyr::case_when(\n        SampleType %in% c(\"Control for KMT2D LOF discovery cohort\", \"Control for validation cohort\") ~ \"Train Control\",\n        SampleType == \"KMT2D LOF discovery cohort\" ~ \"Train Case\",\n        TRUE ~ Diagnosis\n      ),\n      levels = c(\"Train Control\", \"Train Case\", \"Control\", \"KMT2D variant\", \"KDM6A variant\", \"CHARGE\", \"CHD7 variant\")\n    )\n  )\n\nopenxlsx::write.xlsx(\n  predictions_df,\n  file.path(path_save, \"Tables\", paste0(\"SVM_predictions_\", geo_ID, \".xlsx\")),\n  rowNames = FALSE\n)"},{"path":"kabuki.html","id":"prepare-gse116300","chapter":"6 Kabuki Workflow","heading":"6.6 Prepare GSE116300","text":"GSE116300, independent Kabuki cohort, prepared general object structure uses quantile normalization script (Sobreira et al. 2017):script also compares beta-values generated IDAT workflow processed beta-values distributed GEO. PCA provides simple visual check whether two data sources show similar broad structure. comparison descriptive; imply two preprocessing procedures produce identical methylation values.","code":"\ngeo_ID <- \"GSE116300\"\npath_save <- file.path(project_root, \"Kabuki\", geo_ID)\npath_idat <- file.path(path_save, \"IDATs\")\nensure_project_dirs(path_save)\n\nGRset <- read_idat_minfi(\n  path_idat = path_idat,\n  pheno = readr::read_csv(file.path(path_save, paste0(\"pheno_\", geo_ID, \".csv\")), show_col_types = FALSE),\n  path_save = path_save,\n  estimate_cell_counts = FALSE,\n  dataset_name = geo_ID,\n  process_meth = \"quantile\"\n)\n\nGRset_epis <- GRset[rownames(GRset) %in% episignature_cpgs$id, ]\n\ncolData(GRset_epis) <- as.data.frame(SummarizedExperiment::colData(GRset_epis)) %>%\n  dplyr::mutate(\n    Diagnosis = mutation,\n    Age = NA_real_,\n    Sex = predSex\n  ) %>%\n  dplyr::select(title, geo_accession, Case_Cont, Sex, Age, Diagnosis, Dataset) %>%\n  S4Vectors::DataFrame()\n\nsaveRDS(GRset_epis, file.path(path_save, paste0(\"GRset_epis_\", geo_ID, \".rds\")))"},{"path":"kabuki.html","id":"prepare-gse218186","chapter":"6 Kabuki Workflow","heading":"6.7 Prepare GSE218186","text":"GSE218186 provides another independent array-based testing cohort. used define Kabuki CpG panel fit SVM.","code":"\ngeo_ID <- \"GSE218186\"\npath_save <- file.path(project_root, \"Kabuki\", geo_ID)\npath_idat <- file.path(path_save, \"IDATs\")\nensure_project_dirs(path_save)\n\nGRset <- read_idat_minfi(\n  path_idat = path_idat,\n  pheno = readr::read_csv(file.path(path_save, paste0(\"pheno_\", geo_ID, \".csv\")), show_col_types = FALSE),\n  path_save = path_save,\n  estimate_cell_counts = FALSE,\n  dataset_name = geo_ID,\n  process_meth = \"funnorm\"\n)\n\nGRset_epis <- GRset[rownames(GRset) %in% episignature_cpgs$id, ]\n\ncolData(GRset_epis) <- as.data.frame(SummarizedExperiment::colData(GRset_epis)) %>%\n  dplyr::mutate(\n    Diagnosis = dplyr::case_when(Case_Cont == \"Case\" ~ \"Kabuki\", TRUE ~ \"Control\"),\n    Age = NA_real_,\n    Sex = predSex\n  ) %>%\n  dplyr::select(title, geo_accession, Case_Cont, Sex, Age, Diagnosis, Dataset) %>%\n  S4Vectors::DataFrame()\n\nsaveRDS(GRset_epis, file.path(path_save, paste0(\"GRset_epis_\", geo_ID, \".rds\")))"},{"path":"kabuki.html","id":"convert-long-read-beta-workbook-to-a-genomicratioset","chapter":"6 Kabuki Workflow","heading":"6.8 Convert long-read beta workbook to a GenomicRatioSet","text":"long-read script reads methylation workbook Hildonen et al. (Hildonen et al. 2026). file, rows contain sample metadata (Platform, Affected, Cohort) remaining rows contain CpG methylation values. code separates two parts creating GenomicRatioSet.object contains CpGs available workbook. trained classifier can applied required predictor CpGs present. later guard performs check explicitly rather silently scoring incomplete feature set.","code":"\npath_save <- file.path(project_root, \"Kabuki\", \"PMID41225292\")\n\ntable_metadata <- readxl::read_excel(\n  file.path(path_save, \"Kabuki_PB_ONT_PMID41225292.xlsx\")\n)\n\npheno_lr <- table_metadata %>%\n  dplyr::filter(ID %in% c(\"Platform\", \"Affected\", \"Cohort\")) %>%\n  tidyr::pivot_longer(cols = -ID, names_to = \"geo_accession\", values_to = \"value\") %>%\n  tidyr::pivot_wider(names_from = ID, values_from = value) %>%\n  dplyr::mutate(\n    title = sub(\"_.*\", \"\", geo_accession),\n    Case_Cont = dplyr::case_when(Affected == \"Affected\" ~ \"Case\", TRUE ~ \"Control\"),\n    Diagnosis = dplyr::case_when(\n      Platform == \"Array\" & Case_Cont == \"Case\" ~ \"Kabuki\",\n      Platform == \"Array\" & Case_Cont == \"Control\" ~ \"Control\",\n      Case_Cont == \"Case\" ~ paste0(\"Kabuki_\", Platform),\n      Case_Cont == \"Control\" ~ paste0(\"Control_\", Platform)\n    ),\n    Dataset = Platform,\n    Sex = NA_character_,\n    Age = NA_real_\n  ) %>%\n  dplyr::select(title, geo_accession, Case_Cont, Sex, Age, Diagnosis, Dataset) %>%\n  as.data.frame() %>%\n  tibble::column_to_rownames(\"geo_accession\") %>%\n  S4Vectors::DataFrame()\n\nbetas_lr <- table_metadata %>%\n  dplyr::filter(!ID %in% c(\"Platform\", \"Affected\", \"Cohort\")) %>%\n  tibble::column_to_rownames(var = \"ID\") %>%\n  as.matrix()\n\nGRset_lr <- minfi::makeGenomicRatioSetFromMatrix(\n  betas_lr,\n  pData = pheno_lr,\n  what = \"Beta\"\n)\n\nsaveRDS(GRset_lr, file.path(path_save, \"GRset_epis_PMID41225292.rds\"))"},{"path":"kabuki.html","id":"combine-kabuki-datasets","chapter":"6 Kabuki Workflow","heading":"6.9 Combine Kabuki datasets","text":"Kabuki_analysis.R loads prepared GRset_epis_* objects identifies CpGs shared across . common set useful direct cross-dataset visualization, prediction still requires complete predictor set expected trained model.explicit check prevents accidental scoring missing features. combined data contain every CpG used train SVM, code stops. real analysis, solution must defined scoring—example, training new model pre-specified shared CpG set using training data . Missing predictors simply ignored.","code":"\npath_save <- file.path(project_root, \"Kabuki\")\nsvm_mod <- readRDS(file.path(path_save, \"GSE97362\", \"svm_model_GSE97362.rds\"))\n\nlist_files <- list.files(\n  path = path_save,\n  pattern = \"GRset_epis_\",\n  full.names = TRUE,\n  recursive = TRUE\n)\n\nlist_GRsets <- lapply(list_files, function(x) {\n  GRset <- readRDS(x)\n  if (\"CN\" %in% SummarizedExperiment::assayNames(GRset)) {\n    SummarizedExperiment::assay(GRset, \"CN\") <- NULL\n  }\n  GRset\n})\n\ncommon_cpgs <- Reduce(intersect, lapply(list_GRsets, rownames))\n\nmodel_cpgs <- model_predictor_names(svm_mod)\nmissing_model_cpgs <- setdiff(model_cpgs, common_cpgs)\nif (length(missing_model_cpgs) > 0) {\n  stop(\"The combined Kabuki object is missing model CpGs. Do not score until this is resolved.\")\n}\n\nGRset_common <- Reduce(cbind, lapply(list_GRsets, function(x) x[common_cpgs, ]))"},{"path":"kabuki.html","id":"assign-plot-statuses","chapter":"6 Kabuki Workflow","heading":"6.10 Assign plot statuses","text":"plotting interpretation, script separates confirmed training samples independent array samples, ONT/PacBio samples, CHARGE/CHD7 comparators. labels change model; make possible see whether prediction behaviour differs disease group, dataset, platform.","code":"\nGRset_common$Status <- factor(\n  dplyr::case_when(\n    colnames(GRset_common) %in% rownames(svm_mod$trainingData) & GRset_common$Case_Cont == \"Case\" ~ \"Train Kabuki\",\n    colnames(GRset_common) %in% rownames(svm_mod$trainingData) & GRset_common$Case_Cont == \"Control\" ~ \"Train Control\",\n    GRset_common$Diagnosis %in% c(\"CHARGE\", \"CHD7 variant\") ~ \"CHARGE\",\n    GRset_common$Dataset == \"ONT_A\" & GRset_common$Case_Cont == \"Case\" ~ \"Kabuki_ONT_A\",\n    GRset_common$Dataset == \"ONT_WGS\" & GRset_common$Case_Cont == \"Case\" ~ \"Kabuki_ONT_WGS\",\n    GRset_common$Dataset == \"PacBio\" & GRset_common$Case_Cont == \"Case\" ~ \"Kabuki_PacBio\",\n    GRset_common$Dataset == \"ONT_A\" & GRset_common$Case_Cont == \"Control\" ~ \"Control_ONT_A\",\n    GRset_common$Dataset == \"ONT_WGS\" & GRset_common$Case_Cont == \"Control\" ~ \"Control_ONT_WGS\",\n    GRset_common$Dataset == \"PacBio\" & GRset_common$Case_Cont == \"Control\" ~ \"Control_PacBio\",\n    GRset_common$Dataset == \"Array\" & GRset_common$Case_Cont == \"Case\" ~ \"Test Kabuki\",\n    GRset_common$Case_Cont == \"Case\" ~ \"Test Kabuki\",\n    GRset_common$Case_Cont == \"Control\" ~ \"Test Control\"\n  ),\n  levels = c(\n    \"Train Kabuki\", \"Train Control\", \"Test Kabuki\",\n    \"Kabuki_ONT_A\", \"Kabuki_ONT_WGS\", \"Kabuki_PacBio\",\n    \"Test Control\", \"Control_ONT_A\", \"Control_ONT_WGS\", \"Control_PacBio\",\n    \"CHARGE\"\n  )\n)\n\nGRset_common$dataset_id <- GRset_common$Dataset\nGRset_common$dataset_id[GRset_common$dataset_id %in% c(\"PacBio\", \"ONT_A\", \"ONT_WGS\", \"Array\")] <- \"PMID41225292\"\nGRset_common$dataset_id <- factor(\n  GRset_common$dataset_id,\n  levels = c(\"GSE97362\", \"GSE116300\", \"GSE218186\", \"PMID41225292\")\n)"},{"path":"kabuki.html","id":"pca-on-kabuki-episignature-cpgs","chapter":"6 Kabuki Workflow","heading":"6.11 PCA on Kabuki episignature CpGs","text":"","code":"\nbetas <- t(minfi::getBeta(GRset_common[model_cpgs, ]))\n\npca_res <- stats::prcomp(betas, scale. = TRUE)\nvar_explained <- round(100 * pca_res$sdev^2 / sum(pca_res$sdev^2), 2)\n\npca_df <- as.data.frame(pca_res$x) %>%\n  dplyr::select(PC1, PC2) %>%\n  tibble::rownames_to_column(var = \"geo_accession\") %>%\n  dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_common)), by = \"geo_accession\")\n\ncol_pal <- c(\n  \"Train Control\" = \"lightblue\",\n  \"Train Kabuki\" = \"lightcoral\",\n  \"Test Control\" = \"dodgerblue\",\n  \"Control_ONT_A\" = \"cornflowerblue\",\n  \"Control_ONT_WGS\" = \"royalblue\",\n  \"Control_PacBio\" = \"mediumblue\",\n  \"Test Kabuki\" = \"firebrick\",\n  \"Kabuki_ONT_A\" = \"orangered\",\n  \"Kabuki_ONT_WGS\" = \"#7a2607\",\n  \"Kabuki_PacBio\" = \"darkred\",\n  \"CHARGE\" = \"forestgreen\"\n)\n\nshape_pal <- c(\n  \"GSE97362\" = 21,\n  \"GSE116300\" = 22,\n  \"GSE218186\" = 23,\n  \"PMID41225292\" = 24\n)\n\nplt_pca <- ggplot2::ggplot(pca_df, ggplot2::aes(PC1, PC2)) +\n  ggplot2::geom_point(ggplot2::aes(fill = Status, shape = dataset_id), size = 2.5, alpha = 0.75, color = \"black\") +\n  ggplot2::labs(\n    x = paste0(\"PC1 (\", var_explained[1], \"% var)\"),\n    y = paste0(\"PC2 (\", var_explained[2], \"% var)\"),\n    fill = \"Status\",\n    shape = \"Dataset\"\n  ) +\n  ggplot2::scale_fill_manual(values = col_pal) +\n  ggplot2::scale_shape_manual(values = shape_pal) +\n  plot_custom_theme()"},{"path":"kabuki.html","id":"svm-scores-across-datasets","chapter":"6 Kabuki Workflow","heading":"6.12 SVM scores across datasets","text":"score plot answers classification question:confirmed Kabuki array samples receive high case scores expected episignature preserved;controls remain low specificity maintained;CHARGE/CHD7 samples test whether classifier confuses related developmental disorder Kabuki;ONT PacBio samples test whether array-derived Kabuki signal remains detectable long-read methylation estimates.manuscript analysis, molecularly confirmed Kabuki samples controls classified according expected status, variant samples previously interpreted non-pathogenic produced negative results consistent published interpretation. long-read Kabuki control profiles also classified expected. observations demonstrate transfer strong signal example, universal cross-platform equivalence.","code":"\npredictions_df <- predict_model_on_grset(svm_mod, GRset_common) %>%\n  dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_common)), by = \"geo_accession\")\n\nplt_svm <- svm_plt(predictions_df, col_pal = col_pal, shape_pal = shape_pal)\n\nopenxlsx::write.xlsx(\n  predictions_df,\n  file.path(path_save, \"Tables\", \"SVM_predictions_Episignature.xlsx\"),\n  rowNames = FALSE\n)"},{"path":"kabuki.html","id":"long-read-deviation-analysis","chapter":"6 Kabuki Workflow","heading":"6.13 Long-read deviation analysis","text":"final part Kabuki_analysis.R asks different question: even classification correct, far methylation values array-based reference? script:selects high-confidence non-long-read samples: cases Case > 0.75 controls Case < 0.25;randomly uses 80% samples build class-specific array reference profiles;calculates mean beta-value beta-value standard deviation CpG within cases controls;calculates remaining sample’s deviation appropriate class reference;compares deviations across array long-read platforms.0.75/0.25 thresholds used construct clean reference set exploratory comparison. proposed general diagnostic thresholds.compute sample-level deviations:deviation analysis exploratory. manuscript example, long-read profiles show larger deviations array-derived reference independent array samples, matched array profiles remain closer expected array range. supports platform-mediated shift methylation measurements even though Kabuki disease signal still detectable (Hildonen et al. 2026).","code":"\nset.seed(123)\n\nsamples_keep <- predictions_df %>%\n  dplyr::filter(\n    dataset_id != \"PMID41225292\",\n    Status != \"CHARGE\",\n    (Case > 0.75 & Case_Cont == \"Case\") | (Case < 0.25 & Case_Cont == \"Control\")\n  ) %>%\n  dplyr::group_by(Status, Case_Cont) %>%\n  dplyr::slice_sample(prop = 0.8) %>%\n  dplyr::pull(geo_accession)\n\nGRset_reference <- GRset_common[, colnames(GRset_common) %in% samples_keep]\n\nsummary_betas <- as.data.frame(minfi::getBeta(GRset_reference)) %>%\n  tibble::rownames_to_column(var = \"id\") %>%\n  tidyr::pivot_longer(-id, names_to = \"geo_accession\", values_to = \"Beta\") %>%\n  dplyr::mutate(Beta = as.numeric(Beta)) %>%\n  dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_reference)), by = \"geo_accession\") %>%\n  dplyr::group_by(id, Case_Cont) %>%\n  dplyr::reframe(\n    mean_beta = mean(Beta, na.rm = TRUE),\n    sd_beta = stats::sd(Beta, na.rm = TRUE),\n    n = dplyr::n()\n  )\nGRset_valid <- GRset_common[\n  ,\n  !colnames(GRset_common) %in% samples_keep &\n    GRset_common$Status != \"CHARGE\"\n]\n\ndiff_betas <- as.data.frame(minfi::getBeta(GRset_valid)) %>%\n  tibble::rownames_to_column(\"id\") %>%\n  tidyr::pivot_longer(-id, names_to = \"geo_accession\", values_to = \"Beta\") %>%\n  dplyr::mutate(Beta = as.numeric(Beta)) %>%\n  dplyr::left_join(as.data.frame(SummarizedExperiment::colData(GRset_valid)), by = \"geo_accession\") %>%\n  dplyr::left_join(summary_betas, by = c(\"id\", \"Case_Cont\")) %>%\n  dplyr::mutate(\n    beta_diff = Beta - mean_beta,\n    z_score = beta_diff / sd_beta\n  )\n\nsample_deviation <- diff_betas %>%\n  dplyr::group_by(geo_accession, Case_Cont, dataset_id, Status) %>%\n  dplyr::reframe(\n    mean_abs_diff = mean(abs(beta_diff), na.rm = TRUE),\n    mean_abs_z = mean(abs(z_score), na.rm = TRUE),\n    mean_z = mean(z_score, na.rm = TRUE),\n    sd_z = stats::sd(z_score, na.rm = TRUE),\n    n_cpg = dplyr::n()\n  ) %>%\n  dplyr::left_join(predictions_df %>% dplyr::select(geo_accession, Case), by = \"geo_accession\")"},{"path":"kabuki.html","id":"what-kabuki-teaches","chapter":"6 Kabuki Workflow","heading":"6.14 What Kabuki teaches","text":"Kabuki workflow illustrates several validation questions kept separate:fixed CpG panel distinguish Kabuki controls independent array data?remain specific challenged CHARGE/CHD7 comparators?Can disease-associated signal still detected ONT PacBio methylation profiles?underlying methylation values numerically comparable across platforms?Kabuki example, answer classification question encouraging, PCA, clustering, deviation analyses still show systematic technology-related differences. result therefore best viewed proof principle strong established episignature, evidence every array-derived episignature can transferred long-read data without additional validation.","code":""},{"path":"wrapper-functions.html","id":"wrapper-functions","chapter":"7 Wrapper functions","heading":"7 Wrapper functions","text":"chapter collects reusable R functions used throughout tutorial. Keeping functions one place makes practical chapters easier read gives readers single reference code supports preprocessing, model training, prediction, visualization.functions shown correspond helper script used tutorial:practical chapters, file loaded :code chapter shown eval=FALSE. means rendering book displays functions execute . Analyses still use sourceable .R file .Several small safeguards included make wrappers easier reuse outside original analysis environment. particular, output paths passed explicitly, required model CpGs checked prediction, plotting functions avoid machine-specific temporary paths.","code":"R/functions_tutorial_epis.R\nsource(file.path(\"R\", \"functions_tutorial_epis.R\"))"},{"path":"wrapper-functions.html","id":"project-and-package-helpers","chapter":"7 Wrapper functions","heading":"7.1 Project and package helpers","text":"small functions used internally rest wrapper file. .require_package() gives clear error required package unavailable, ensure_project_dirs() creates output folders expected tutorial.","code":"\n.require_package <- function(pkg) {\n  if (!requireNamespace(pkg, quietly = TRUE)) {\n    stop(\"Package '\", pkg, \"' is required. Please install it first.\", call. = FALSE)\n  }\n  invisible(TRUE)\n}\n\nensure_project_dirs <- function(path_save) {\n  dirs <- c(\n    path_save,\n    file.path(path_save, \"IDATs\"),\n    file.path(path_save, \"Tables\"),\n    file.path(path_save, \"Figures\"),\n    file.path(path_save, \"tmp_data\")\n  )\n  invisible(lapply(dirs, dir.create, recursive = TRUE, showWarnings = FALSE))\n}"},{"path":"wrapper-functions.html","id":"reading-and-preprocessing-methylation-arrays","chapter":"7 Wrapper functions","heading":"7.2 Reading and preprocessing methylation arrays","text":"read_idat_minfi() performs main preprocessing steps used array datasets tutorial. reads raw IDAT files, applies sample-level detection P-value filter, joins phenotype information, predicts sex quality control, normalizes methylation data, removes probes overlapping common SNPs cross-reactive probes, applies probe-level detection P-value filter.requested, also estimates blood-cell proportions writes tutorial output directory. function returns processed GenomicRatioSet downstream analyses.quality-control thresholds used reproduce tutorial workflow interpreted universal thresholds every methylation study.","code":"\nread_idat_minfi <- function(\n  path_idat,\n  pheno,\n  path_save = NULL,\n  estimate_cell_counts = TRUE,\n  dataset_name = \"\",\n  process_meth = \"funnorm\"\n) {\n  .require_package(\"minfi\")\n  .require_package(\"dplyr\")\n  .require_package(\"tibble\")\n  .require_package(\"S4Vectors\")\n  .require_package(\"SummarizedExperiment\")\n  .require_package(\"maxprobes\")\n\n  if (!dir.exists(path_idat)) {\n    stop(\"IDAT directory does not exist: \", path_idat, call. = FALSE)\n  }\n  if (!\"geo_accession\" %in% colnames(pheno)) {\n    stop(\"`pheno` must contain a `geo_accession` column.\", call. = FALSE)\n  }\n\n  process_meth <- match.arg(process_meth, c(\"funnorm\", \"noob\", \"quantile\"))\n\n  message(\"Reading IDATs...\")\n  RGset <- minfi::read.metharray.exp(base = path_idat)\n\n  message(\"Sample detection P-values...\")\n  detP <- minfi::detectionP(RGset)\n  sample_detP <- colMeans(detP, na.rm = TRUE)\n  keep_samples <- sample_detP < 0.05\n  message(\"Keeping \", sum(keep_samples), \" / \", length(keep_samples), \" samples.\")\n\n  if (!any(keep_samples)) {\n    stop(\"No samples passed the mean detection P-value < 0.05 filter.\", call. = FALSE)\n  }\n  RGset <- RGset[, keep_samples]\n\n  sample_meta <- data.frame(\n    geo_accession = sub(\"_.*\", \"\", colnames(RGset)),\n    idat_name = colnames(RGset),\n    stringsAsFactors = FALSE\n  ) |>\n    dplyr::left_join(pheno, by = \"geo_accession\")\n\n  rownames(sample_meta) <- sample_meta$geo_accession\n  SummarizedExperiment::colData(RGset) <- S4Vectors::DataFrame(sample_meta)\n\n  message(\"Predicting sex for QC...\")\n  MSet_qc <- minfi::preprocessNoob(RGset)\n  MSet_qc <- minfi::mapToGenome(MSet_qc)\n\n  predSex <- as.data.frame(minfi::getSex(MSet_qc)) |>\n    tibble::rownames_to_column(var = \"sample_id\") |>\n    dplyr::mutate(\n      geo_accession = sub(\"_.*\", \"\", sample_id),\n      predSex = dplyr::case_when(\n        predictedSex == \"M\" ~ \"Male\",\n        predictedSex == \"F\" ~ \"Female\",\n        TRUE ~ NA_character_\n      )\n    ) |>\n    dplyr::select(geo_accession, predictedSex, predSex)\n\n  sample_meta <- as.data.frame(SummarizedExperiment::colData(RGset)) |>\n    tibble::rownames_to_column(var = \".row_id\") |>\n    dplyr::left_join(predSex, by = \"geo_accession\")\n  rownames(sample_meta) <- sample_meta$.row_id\n  sample_meta$.row_id <- NULL\n  SummarizedExperiment::colData(RGset) <- S4Vectors::DataFrame(sample_meta)\n\n  message(\"Processing the data...\")\n  if (process_meth == \"funnorm\") {\n    message(\"Using functional normalization...\")\n    GRset <- minfi::preprocessFunnorm(RGset, sex = RGset$predictedSex)\n  } else if (process_meth == \"noob\") {\n    message(\"Using noob normalization...\")\n    GRset <- minfi::preprocessNoob(RGset)\n  } else {\n    message(\"Using quantile normalization...\")\n    GRset <- minfi::preprocessQuantile(RGset)\n  }\n\n  message(\"Dropping loci with SNPs...\")\n  GRset <- minfi::dropLociWithSnps(GRset, maf = 0.01)\n\n  message(\"Dropping cross-reactive probes...\")\n  GRset <- maxprobes::dropXreactiveLoci(GRset)\n\n  # For other array versions, consider platform-specific masks, e.g. sesame.\n\n  message(\"Filtering probes with low detection P-values...\")\n  GRset <- GRset[intersect(rownames(GRset), rownames(detP)), ]\n  detP_subset <- detP[rownames(GRset), GRset$idat_name, drop = FALSE]\n  keep_probes <- rowSums(detP_subset < 0.01, na.rm = TRUE) == ncol(GRset)\n  GRset <- GRset[keep_probes, ]\n\n  GRset$Dataset <- dataset_name\n\n  if (estimate_cell_counts) {\n    .require_package(\"FlowSorted.Blood.EPIC\")\n    .require_package(\"readr\")\n\n    if (is.null(path_save)) {\n      stop(\"`path_save` must be supplied when `estimate_cell_counts = TRUE`.\", call. = FALSE)\n    }\n    ensure_project_dirs(path_save)\n\n    message(\"Estimating immune cell composition...\")\n    cell_estimates <- FlowSorted.Blood.EPIC::estimateCellCounts2(\n      RGset,\n      compositeCellType = \"Blood\",\n      processMethod = \"preprocessNoob\",\n      probeSelect = \"IDOL\",\n      cellTypes = c(\"CD8T\", \"CD4T\", \"NK\", \"Bcell\", \"Mono\", \"Neu\")\n    )\n\n    cell_counts <- as.data.frame(cell_estimates$prop) |>\n      tibble::rownames_to_column(var = \"geo_accession\") |>\n      dplyr::mutate(geo_accession = sub(\"_.*\", \"\", geo_accession))\n\n    readr::write_csv(\n      cell_counts,\n      file.path(path_save, \"Tables\", paste0(\"CellCounts_\", dataset_name, \".csv\"))\n    )\n  }\n\n  message(\"Finished processing the data.\")\n  GRset\n}"},{"path":"wrapper-functions.html","id":"shared-plotting-theme","chapter":"7 Wrapper functions","heading":"7.3 Shared plotting theme","text":"plot_custom_theme() provides common ggplot2 theme figures consistent appearance across tutorial.","code":"\nplot_custom_theme <- function() {\n  .require_package(\"ggplot2\")\n  ggplot2::theme_classic() +\n    ggplot2::theme(\n      legend.position = \"right\",\n      axis.text = ggplot2::element_text(size = 13),\n      axis.title = ggplot2::element_text(size = 15, face = \"bold\"),\n      legend.text = ggplot2::element_text(size = 10),\n      legend.title = ggplot2::element_text(size = 12, face = \"bold\"),\n      plot.title = ggplot2::element_text(size = 17, face = \"bold\", hjust = 0.5)\n    )\n}"},{"path":"wrapper-functions.html","id":"pca-based-outlier-identification","chapter":"7 Wrapper functions","heading":"7.4 PCA-based outlier identification","text":"detect_pca_outliers() calculates Mahalanobis distance using first two principal components within case/control group. Williams syndrome workflow, resulting flag used identify atypical profiles can reserved validation.Importantly, outlier flag exploratory statistical result. interpreted evidence biological sample poor quality discarded.","code":"\ndetect_pca_outliers <- function(\n  pca_df,\n  conf_level = 0.95,\n  return_all = TRUE\n) {\n  .require_package(\"dplyr\")\n\n  required <- c(\"PC1\", \"PC2\", \"Case_Cont\")\n  missing <- setdiff(required, colnames(pca_df))\n  if (length(missing) > 0) {\n    stop(\"Missing PCA columns: \", paste(missing, collapse = \", \"), call. = FALSE)\n  }\n\n  chi_cutoff <- stats::qchisq(conf_level, df = 2)\n\n  result <- pca_df |>\n    dplyr::group_by(Case_Cont) |>\n    dplyr::group_modify(function(.x, .y) {\n      xy <- cbind(.x$PC1, .x$PC2)\n      if (nrow(.x) < 3) {\n        .x$D2 <- NA_real_\n        .x$outside <- NA\n        return(.x)\n      }\n      d2 <- tryCatch(\n        stats::mahalanobis(\n          xy,\n          center = c(mean(.x$PC1), mean(.x$PC2)),\n          cov = stats::cov(xy)\n        ),\n        error = function(e) rep(NA_real_, nrow(.x))\n      )\n      .x$D2 <- d2\n      .x$outside <- d2 > chi_cutoff\n      .x\n    }) |>\n    dplyr::ungroup()\n\n  if (return_all) result else dplyr::filter(result, outside %in% TRUE)\n}"},{"path":"wrapper-functions.html","id":"preparing-binary-classification-models","chapter":"7 Wrapper functions","heading":"7.5 Preparing binary classification models","text":"following internal functions convert methylation values sample--feature format required caret, create binary case/control outcome, calculate class weights, perform repeated cross-validation hyperparameter selection, fit final model.outcome coded Case positive class. important interpreting ROC-based tuning probability column returned prediction.","code":"\n.prepare_binary_df <- function(GRset_sbst, beta_M = \"Beta\") {\n  .require_package(\"minfi\")\n  .require_package(\"SummarizedExperiment\")\n\n  beta_M <- match.arg(beta_M, c(\"Beta\", \"M\"))\n\n  if (!\"Case_Cont\" %in% colnames(SummarizedExperiment::colData(GRset_sbst))) {\n    stop(\"`GRset_sbst` must contain a `Case_Cont` column.\", call. = FALSE)\n  }\n  if (anyNA(GRset_sbst$Case_Cont)) {\n    stop(\"`Case_Cont` contains missing values in the training set.\", call. = FALSE)\n  }\n  if (!all(as.character(GRset_sbst$Case_Cont) %in% c(\"Case\", \"Control\"))) {\n    stop(\"`Case_Cont` must contain only 'Case' and 'Control'.\", call. = FALSE)\n  }\n\n  message(\"Using assay: \", beta_M)\n  X <- if (beta_M == \"Beta\") {\n    t(minfi::getBeta(GRset_sbst))\n  } else {\n    t(minfi::getM(GRset_sbst))\n  }\n\n  df <- as.data.frame(X, check.names = FALSE)\n  df$Diagnosis <- factor(\n    as.character(GRset_sbst$Case_Cont),\n    levels = c(\"Case\", \"Control\")\n  )\n  df\n}\n\n.compute_class_weights <- function(y) {\n  tbl <- table(y)\n  if (!all(c(\"Case\", \"Control\") %in% names(tbl))) {\n    stop(\"Both Case and Control samples are required.\", call. = FALSE)\n  }\n  n <- sum(tbl)\n  w_case <- n / (2 * tbl[[\"Case\"]])\n  w_control <- n / (2 * tbl[[\"Control\"]])\n  w_case <- as.numeric(w_case / w_control)\n  list(Case = w_case, Control = 1)\n}\n\n.train_binary_model <- function(\n  GRset_sbst,\n  method,\n  tune_grid,\n  repeats = 10,\n  number = 5,\n  sampling = NULL,\n  pre_process = c(\"center\", \"scale\"),\n  n_cores = 5,\n  beta_M = \"Beta\"\n) {\n  .require_package(\"caret\")\n  .require_package(\"doParallel\")\n  .require_package(\"foreach\")\n\n  message(\"*** Training \", method, \" ***\")\n  train_df <- .prepare_binary_df(GRset_sbst, beta_M)\n\n  class_weights <- .compute_class_weights(train_df$Diagnosis)\n  sample_weights <- ifelse(\n    train_df$Diagnosis == \"Case\",\n    class_weights$Case,\n    class_weights$Control\n  )\n\n  n_cores <- max(1L, as.integer(n_cores))\n  if (n_cores > 1L) {\n    cl <- parallel::makePSOCKcluster(n_cores)\n    doParallel::registerDoParallel(cl)\n    on.exit({\n      try(parallel::stopCluster(cl), silent = TRUE)\n      foreach::registerDoSEQ()\n    }, add = TRUE)\n  } else {\n    foreach::registerDoSEQ()\n  }\n\n  tune_control <- caret::trainControl(\n    method = \"repeatedcv\",\n    number = number,\n    repeats = repeats,\n    classProbs = TRUE,\n    sampling = sampling,\n    summaryFunction = caret::twoClassSummary\n  )\n\n  extra_args <- list()\n  if (method == \"svmLinear2\") {\n    extra_args$class.weights <- class_weights\n  } else if (method == \"glmnet\") {\n    extra_args$weights <- sample_weights\n  } else if (method == \"ranger\") {\n    extra_args$case.weights <- sample_weights\n  } else if (method == \"nnet\") {\n    extra_args$trace <- FALSE\n    extra_args$maxit <- 500\n  }\n\n  mod_cv <- do.call(caret::train, c(\n    list(\n      Diagnosis ~ .,\n      data = train_df,\n      method = method,\n      trControl = tune_control,\n      tuneGrid = tune_grid,\n      preProcess = pre_process,\n      metric = \"ROC\"\n    ),\n    extra_args\n  ))\n\n  best_grid <- mod_cv$bestTune\n\n  final_control <- caret::trainControl(method = \"none\", classProbs = TRUE)\n  mod_final <- do.call(caret::train, c(\n    list(\n      Diagnosis ~ .,\n      data = train_df,\n      method = method,\n      trControl = final_control,\n      tuneGrid = best_grid,\n      preProcess = pre_process\n    ),\n    extra_args\n  ))\n\n  mod_final$class_weights <- class_weights\n  mod_final$sample_weights <- sample_weights\n  mod_final$beta_M <- beta_M\n  mod_final$bestTune_cv <- best_grid\n  mod_final$tuning_results <- mod_cv$results\n  mod_final\n}"},{"path":"wrapper-functions.html","id":"machine-learning-model-wrappers","chapter":"7 Wrapper functions","heading":"7.6 Machine-learning model wrappers","text":"functions provide consistent interface classifiers evaluated Williams syndrome practical. include linear support vector machine, elastic-net regression, random forest ranger, k-nearest neighbours, partial least squares, naive Bayes, gradient boosting, XGBoost, neural network.tutorial compares several algorithms demonstrate classifier implementation. Strong performance Williams example reflects strength particular methylation signal taken mean algorithms perform equally well every episignature.","code":"\nsvm_training_f <- function(\n  GRset_sbst,\n  repeats = 10,\n  number = 5,\n  sampling = NULL,\n  beta_M = \"Beta\",\n  n_cores = 5\n) {\n  svm_grid <- expand.grid(\n    cost = 10^seq(-4, 4, length.out = 40) # Cost for the linear SVM margin penalty.\n  )\n  .train_binary_model(\n    GRset_sbst, \"svmLinear2\", svm_grid,\n    repeats, number, sampling,\n    c(\"center\", \"scale\"), n_cores, beta_M\n  )\n}\n\nelastic_net_training_f <- function(\n  GRset_sbst, repeats = 10, number = 5, sampling = NULL,\n  beta_M = \"Beta\", n_cores = 5\n) {\n  message(\"-- Elastic net training --\")\n  glmnet_grid <- expand.grid(\n    alpha = seq(0, 1, by = 0.1),\n    lambda = 10^seq(-5, 1, length.out = 50)\n  )\n  .train_binary_model(\n    GRset_sbst, \"glmnet\", glmnet_grid,\n    repeats, number, sampling, NULL, n_cores, beta_M\n  )\n}\n\nranger_training_f <- function(\n  GRset_sbst, repeats = 10, number = 5, sampling = NULL,\n  beta_M = \"Beta\", n_cores = 5\n) {\n  message(\"-- Ranger training --\")\n  p <- ncol(.prepare_binary_df(GRset_sbst, beta_M)) - 1\n  ranger_grid <- expand.grid(\n    mtry = unique(round(seq(sqrt(p), min(500, p), length.out = 8))),\n    splitrule = \"gini\",\n    min.node.size = c(1, 5, 10)\n  )\n  .train_binary_model(\n    GRset_sbst, \"ranger\", ranger_grid,\n    repeats, number, sampling, NULL, n_cores, beta_M\n  )\n}\n\nknn_training_f <- function(\n  GRset_sbst, repeats = 10, number = 5, sampling = NULL,\n  beta_M = \"Beta\", n_cores = 5\n) {\n  message(\"-- KNN training --\")\n  train_df <- .prepare_binary_df(GRset_sbst, beta_M)\n  n_samples <- nrow(train_df)\n  n_minority <- min(table(train_df$Diagnosis))\n  k_upper <- min(ceiling(1.5 * sqrt(n_samples)), floor(0.8 * n_minority))\n  k_upper <- max(k_upper, 3)\n  k_seq <- seq(3, k_upper, by = 2)\n  if (length(k_seq) == 0) k_seq <- 3\n  knn_grid <- expand.grid(kmax = k_seq, distance = 2, kernel = \"optimal\")\n  .train_binary_model(\n    GRset_sbst, \"kknn\", knn_grid,\n    repeats, number, sampling,\n    c(\"center\", \"scale\"), n_cores, beta_M\n  )\n}\n\npls_training_f <- function(\n  GRset_sbst, repeats = 10, number = 5, sampling = NULL,\n  beta_M = \"Beta\", n_cores = 5\n) {\n  message(\"-- PLS training --\")\n  max_ncomp <- min(20, ncol(GRset_sbst) - 1, nrow(GRset_sbst))\n  pls_grid <- expand.grid(ncomp = seq_len(max(1, max_ncomp)))\n  .train_binary_model(\n    GRset_sbst, \"pls\", pls_grid,\n    repeats, number, sampling,\n    c(\"center\", \"scale\"), n_cores, beta_M\n  )\n}\n\nnb_training_f <- function(\n  GRset_sbst, repeats = 10, number = 5, sampling = NULL,\n  beta_M = \"Beta\", n_cores = 5\n) {\n  message(\"-- Naive Bayes training --\")\n  nb_grid <- expand.grid(\n    usekernel = c(TRUE, FALSE),\n    fL = c(0, 0.5, 1),\n    adjust = c(0.5, 1, 2, 3, 5)\n  )\n  .train_binary_model(\n    GRset_sbst, \"nb\", nb_grid,\n    repeats, number, sampling,\n    c(\"center\", \"scale\"), n_cores, beta_M\n  )\n}\n\ngbm_training_f <- function(\n  GRset_sbst, repeats = 10, number = 5, sampling = NULL,\n  beta_M = \"Beta\", n_cores = 5\n) {\n  message(\"-- GBM training --\")\n  gbm_grid <- expand.grid(\n    n.trees = c(100, 250, 500),\n    interaction.depth = c(1, 2, 3),\n    shrinkage = c(0.01, 0.05, 0.1),\n    n.minobsinnode = 10\n  )\n  .train_binary_model(\n    GRset_sbst, \"gbm\", gbm_grid,\n    repeats, number, sampling, NULL, n_cores, beta_M\n  )\n}\n\nxgb_training_f <- function(\n  GRset_sbst, repeats = 10, number = 5, sampling = NULL,\n  beta_M = \"Beta\", n_cores = 5\n) {\n  message(\"-- XGBoost training --\")\n  xgb_grid <- expand.grid(\n    nrounds = c(100, 250),\n    max_depth = c(2, 4, 6),\n    eta = c(0.01, 0.05, 0.1),\n    gamma = 0,\n    colsample_bytree = 0.8,\n    min_child_weight = 1,\n    subsample = 0.8\n  )\n  .train_binary_model(\n    GRset_sbst, \"xgbTree\", xgb_grid,\n    repeats, number, sampling, NULL, n_cores, beta_M\n  )\n}\n\nnnet_training_f <- function(\n  GRset_sbst, repeats = 10, number = 5, sampling = NULL,\n  beta_M = \"Beta\", n_cores = 5\n) {\n  message(\"-- Neural Network training --\")\n  nnet_grid <- expand.grid(\n    size = c(1, 3, 5, 7, 10),\n    decay = 10^seq(-5, -1, length.out = 5)\n  )\n  .train_binary_model(\n    GRset_sbst, \"nnet\", nnet_grid,\n    repeats, number, sampling,\n    c(\"center\", \"scale\"), n_cores, beta_M\n  )\n}"},{"path":"wrapper-functions.html","id":"predicting-new-samples","chapter":"7 Wrapper functions","heading":"7.7 Predicting new samples","text":"External validation requires new dataset contain CpG predictors used train model. model_predictor_names() retrieves CpGs, predict_model_on_grset() checks present calculating class probabilities.check prevents common error feature set silently reduced intersection two datasets. fitted classifier applied dropping predictors unless model explicitly retrained validated imputation strategy defined.","code":"\nmodel_predictor_names <- function(model) {\n  predictors <- model$coefnames\n  if (is.null(predictors) || length(predictors) == 0) {\n    if (!is.null(model$trainingData)) {\n      predictors <- setdiff(colnames(model$trainingData), \".outcome\")\n    }\n  }\n  if (is.null(predictors) || length(predictors) == 0) {\n    stop(\"Could not determine model predictor names.\", call. = FALSE)\n  }\n  predictors\n}\n\npredict_model_on_grset <- function(model, GRset, beta_M = NULL) {\n  .require_package(\"minfi\")\n  .require_package(\"tibble\")\n\n  model_cpgs <- model_predictor_names(model)\n  missing_cpgs <- setdiff(model_cpgs, rownames(GRset))\n\n  if (length(missing_cpgs) > 0) {\n    stop(\n      \"External data are missing \", length(missing_cpgs),\n      \" model CpG(s). Missing examples: \",\n      paste(utils::head(missing_cpgs, 10), collapse = \", \"),\n      \". Retrain on common CpGs or define a validated imputation strategy.\",\n      call. = FALSE\n    )\n  }\n\n  if (is.null(beta_M)) beta_M <- model$beta_M\n  if (is.null(beta_M)) beta_M <- \"Beta\"\n  beta_M <- match.arg(beta_M, c(\"Beta\", \"M\"))\n\n  X <- if (beta_M == \"Beta\") {\n    t(minfi::getBeta(GRset[model_cpgs, , drop = FALSE]))\n  } else {\n    t(minfi::getM(GRset[model_cpgs, , drop = FALSE]))\n  }\n\n  newdata <- as.data.frame(X, check.names = FALSE)\n  newdata <- newdata[, model_cpgs, drop = FALSE]\n\n  probs <- stats::predict(model, newdata = newdata, type = \"prob\")\n  pred <- stats::predict(model, newdata = newdata, type = \"raw\")\n\n  out <- tibble::tibble(\n    geo_accession = rownames(newdata),\n    Prediction = as.character(pred)\n  )\n  if (\"Case\" %in% colnames(probs)) out$Case <- probs$Case\n  if (\"Control\" %in% colnames(probs)) out$Control <- probs$Control\n  out\n}"},{"path":"wrapper-functions.html","id":"plotting-and-heatmap-functions","chapter":"7 Wrapper functions","heading":"7.8 Plotting and heatmap functions","text":"final group functions creates classifier probability plots, hierarchical-clustering heatmaps, categorical colour palettes used worked examples.functions used visualization. determine episignature change classifier predictions.","code":"\nsvm_plt <- function(predictions_df, col_pal, shape_pal) {\n  .require_package(\"ggplot2\")\n  .require_package(\"ggbeeswarm\")\n\n  required <- c(\"Status\", \"Case\", \"dataset_id\")\n  missing <- setdiff(required, colnames(predictions_df))\n  if (length(missing) > 0) {\n    stop(\"Missing plot columns: \", paste(missing, collapse = \", \"), call. = FALSE)\n  }\n\n  plot_df <- predictions_df\n  status_levels <- names(col_pal)[names(col_pal) %in% unique(as.character(plot_df$Status))]\n  plot_df$Status <- factor(plot_df$Status, levels = status_levels)\n\n  ggplot2::ggplot(plot_df, ggplot2::aes(x = Status, y = Case, fill = Status)) +\n    ggplot2::geom_vline(\n      xintercept = seq_along(status_levels),\n      color = \"gray\", linewidth = 0.5, alpha = 0.75\n    ) +\n    ggplot2::geom_hline(\n      yintercept = c(0, 0.25, 0.5, 0.75, 1),\n      linetype = \"dashed\", color = \"gray\", linewidth = 0.3, alpha = 0.75\n    ) +\n    ggbeeswarm::geom_quasirandom(\n      ggplot2::aes(shape = dataset_id),\n      size = 3, color = \"black\", varwidth = TRUE, width = 0.25, alpha = 0.75\n    ) +\n    ggplot2::labs(x = \"Group\", y = \"Case probability\") +\n    ggplot2::scale_fill_manual(values = col_pal, drop = FALSE) +\n    ggplot2::scale_shape_manual(values = shape_pal) +\n    plot_custom_theme() +\n    ggplot2::theme(\n      legend.position = \"none\",\n      axis.text.x = ggplot2::element_text(angle = 45, hjust = 1)\n    )\n}\n\nhclust_plt <- function(\n  betas,\n  pheno,\n  scale_betas = FALSE,\n  target_names = FALSE,\n  condition_cols,\n  dataset_cols,\n  sample_label_cols = NULL\n) {\n  .require_package(\"ComplexHeatmap\")\n  .require_package(\"circlize\")\n  .require_package(\"grid\")\n\n  mat <- as.data.frame(betas)\n  meta <- as.data.frame(pheno)\n  rownames(meta) <- meta$geo_accession\n  meta <- meta[rownames(mat), , drop = FALSE]\n  stopifnot(identical(rownames(meta), rownames(mat)))\n\n  ha <- ComplexHeatmap::HeatmapAnnotation(\n    Condition = meta$Status,\n    Dataset = meta$dataset_id,\n    Score = meta$Case,\n    col = list(\n      Condition = condition_cols,\n      Dataset = dataset_cols,\n      Score = circlize::colorRamp2(c(0, 0.5, 1), c(\"#D73027\", \"#F7F7F7\", \"#1A9850\"))\n    ),\n    na_col = \"grey90\"\n  )\n\n  if (scale_betas) {\n    message(\"Scaling betas\")\n    beta_col_fun <- circlize::colorRamp2(c(-2, 0, 2), c(\"#2EC4B6\", \"#1D3557\", \"#FF4D8D\"))\n    mat <- t(scale(t(mat)))\n  } else {\n    message(\"Not scaling betas\")\n    beta_col_fun <- circlize::colorRamp2(c(0, 0.5, 1), c(\"#8C510A\", \"#121111\", \"#01665E\"))\n    mat <- as.matrix(mat)\n  }\n\n  if (!isFALSE(target_names)) {\n    all_names <- rownames(mat)\n    all_names[!all_names %in% target_names] <- \"\"\n    if (is.null(sample_label_cols)) {\n      samples_col <- rep(\"black\", nrow(mat))\n    } else {\n      samples_col <- unname(sample_label_cols[as.character(meta$Status)])\n      samples_col[is.na(samples_col)] <- \"black\"\n    }\n  } else {\n    all_names <- rep(\"\", nrow(mat))\n    samples_col <- rep(\"black\", nrow(mat))\n  }\n\n  ht <- ComplexHeatmap::Heatmap(\n    t(mat),\n    name = \"Betas\",\n    col = beta_col_fun,\n    top_annotation = ha,\n    cluster_columns = TRUE,\n    cluster_rows = TRUE,\n    show_row_names = FALSE,\n    show_column_names = TRUE,\n    column_labels = all_names,\n    column_names_rot = 45,\n    column_names_gp = grid::gpar(fontsize = 5, col = samples_col),\n    heatmap_legend_param = list(\n      title = \"Betas\",\n      direction = \"horizontal\",\n      legend_width = grid::unit(5, \"cm\")\n    )\n  )\n\n  ComplexHeatmap::draw(\n    ht,\n    heatmap_legend_side = \"bottom\",\n    annotation_legend_side = \"left\"\n  )\n}\n\nhclust_plt_williams <- function(\n  betas,\n  pheno,\n  scale_betas = FALSE,\n  target_names = FALSE,\n  condition_cols\n) {\n  .require_package(\"ComplexHeatmap\")\n  .require_package(\"circlize\")\n  .require_package(\"grid\")\n\n  mat <- as.data.frame(t(betas))\n  meta <- as.data.frame(pheno)\n  rownames(meta) <- meta$geo_accession\n  meta <- meta[rownames(mat), , drop = FALSE]\n  stopifnot(identical(rownames(meta), rownames(mat)))\n\n  ha <- ComplexHeatmap::HeatmapAnnotation(\n    Condition = meta$Status,\n    col = list(Condition = condition_cols)\n  )\n\n  if (scale_betas) {\n    message(\"Scaling betas\")\n    beta_col_fun <- circlize::colorRamp2(c(-2, 0, 2), c(\"#2EC4B6\", \"#1D3557\", \"#FF4D8D\"))\n    mat <- t(scale(t(mat)))\n  } else {\n    message(\"Not scaling betas\")\n    beta_col_fun <- circlize::colorRamp2(c(0, 0.5, 1), c(\"#3939e0\", \"#121111\", \"#c01313\"))\n    mat <- as.matrix(mat)\n  }\n\n  ht <- ComplexHeatmap::Heatmap(\n    t(mat),\n    name = \"Betas\",\n    col = beta_col_fun,\n    top_annotation = ha,\n    cluster_columns = TRUE,\n    cluster_rows = TRUE,\n    show_row_names = FALSE,\n    show_column_names = FALSE,\n    heatmap_legend_param = list(\n      title = \"Beta-value\",\n      direction = \"horizontal\",\n      legend_width = grid::unit(5, \"cm\")\n    )\n  )\n\n  ComplexHeatmap::draw(\n    ht,\n    heatmap_legend_side = \"bottom\",\n    annotation_legend_side = \"left\"\n  )\n}\n\nmake_brewer_palette <- function(f, palette = \"Set2\") {\n  .require_package(\"RColorBrewer\")\n  f <- factor(f)\n  n <- nlevels(f)\n  if (n == 0) return(stats::setNames(character(0), character(0)))\n\n  info <- RColorBrewer::brewer.pal.info\n  if (!palette %in% rownames(info)) {\n    stop(\"Unknown RColorBrewer palette: \", palette, call. = FALSE)\n  }\n\n  max_n <- info[palette, \"maxcolors\"]\n  if (n <= max_n) {\n    cols <- RColorBrewer::brewer.pal(max(3, n), palette)[seq_len(n)]\n  } else {\n    cols <- grDevices::colorRampPalette(\n      RColorBrewer::brewer.pal(max_n, palette)\n    )(n)\n  }\n  stats::setNames(cols, levels(f))\n}"},{"path":"wrapper-functions.html","id":"using-this-file-in-the-tutorial","chapter":"7 Wrapper functions","heading":"7.9 Using this file in the tutorial","text":"Bookdown project, keep files:.R file sourced analysis chapters. .Rmd file included book readers can inspect supporting functions without leaving tutorial.chapter listed _bookdown.yml, example:Bookdown render chapter following Kabuki workflow. filename controls position book; chapter title shown readers comes first-level heading top file.","code":"R/functions_tutorial_epis.R\n07-wrapper-functions.Rmdrmd_files:\n  - index.Rmd\n  - 01-primer.Rmd\n  - 02-repository-map.Rmd\n  - 03-setup-data.Rmd\n  - 04-preprocessing-qc.Rmd\n  - 05-williams-workflow.Rmd\n  - 06-kabuki-workflow.Rmd\n  - 07-wrapper-functions.Rmd\n  - 10-references.Rmd"},{"path":"references.html","id":"references","chapter":"Reference list","heading":"Reference list","text":"","code":""}]
